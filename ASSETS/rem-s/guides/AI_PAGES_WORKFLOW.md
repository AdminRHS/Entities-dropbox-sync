# AI Workflow Instructions: Робота зі сторінками Strapi (Single Types)

## Мета цього документа

Цей файл містить інструкції для AI-асистента про те, як допомагати користувачам працювати зі сторінками Strapi (single types) через скрипти оновлення.

**КРИТИЧНО ВАЖЛИВО:**
- AI **САМ** виконує всі команди в терміналі
- AI **НЕ** просить користувача виконувати команди
- AI працює з файлами напряму через інструменти
- AI задає питання і одразу діє на основі відповідей

---

## ⚠️ ОБОВ'ЯЗКОВА ПЕРЕВІРКА ПЕРЕД ПОЧАТКОМ РОБОТИ

**Коли користувач звертається до тебе з просьбою про сторінки, ОДРАЗУ запитай:**

> "Перед початком роботи потрібно перевірити дві важливі речі:
> 
> **1. Чи встановлено Node.js на вашому комп'ютері?**
> - Так
> - Ні
> 
> **2. Чи є у вас API токен для Strapi?**
> - Так
> - Ні"

### Якщо Node.js НЕ встановлено:

**Одразу надай інструкції:**

> "Node.js обов'язковий для роботи зі скриптами. Без нього скрипти не запустяться.
> 
> **Як встановити Node.js:**
> 1. Відкрийте https://nodejs.org/
> 2. Завантажте LTS версію (рекомендовано)
> 3. Встановіть, використовуючи стандартні налаштування
> 4. Перезапустіть комп'ютер (якщо потрібно)
> 
> **Як перевірити, чи встановлено:**
> - Відкрийте командний рядок (CMD або PowerShell)
> - Введіть: `node --version`
> - Якщо побачите версію (наприклад, v18.17.0) - все встановлено ✅
> 
> **Одразу запитай:** "Встановили Node.js? Якщо так, перейдіть до наступного кроку."

### Якщо API токен НЕ має:

**Одразу надай інструкції:**

> "API токен обов'язковий для роботи з Strapi. Без токена не виконається нічого.
> 
> **Як отримати токен:**
> 1. Відкрийте https://strapi.rem-s.com/admin
> 2. Увійдіть в систему
> 3. Зліва в меню: **Settings** → **API Tokens**
> 4. Натисніть **Create new API Token**
> 5. Заповніть:
>    - **Name**: `Мій токен` (або будь-яка назва)
>    - **Token duration**: `Unlimited` (або оберіть термін)
>    - **Token type**: `Full access` (обов'язково для оновлення!)
> 6. Натисніть **Save**
> 7. **Скопіюйте токен** (він показується один раз!)
> 8. Збережіть токен в безпечному місці
> 
> **⚠️ ВАЖЛИВО:** Токен - це ваш персональний ключ доступу. Не діліться ним з іншими!
> 
> **Одразу запитай:** "Отримали токен? Якщо так, збережіть його і перейдіть до наступного кроку."

### Якщо обидві перевірки пройдено:

**Одразу переходь до основного питання:**

> "Відмінно! Тепер можемо працювати зі сторінками.
> 
> **Що ви хочете зробити?**
> - Оновити дані на сторінці (редагувати текст, змінити контент)
> - Додати нові дані на сторінку
> - Видалити дані зі сторінки
> - Оновити всі сторінки одразу
> - Перевірити, які дані зараз на сайті"

---

## 📋 Структура сторінок (Single Types)

### Доступні сторінки:

1. **About** (`about-us`) - Сторінка "Про нас"
2. **Contact** (`contact`) - Сторінка "Контакти"
3. **Header** (`header`) - Хедер сайту
4. **Home** (`home-page`) - Головна сторінка
5. **NotFound** (`not-found`) - Сторінка 404
6. **ThankYou** (`thank-you`) - Сторінка подяки
7. **VacancyListData** (`vacancy-list-data`) - Дані для списку вакансій
8. **VacancyPage** (`vacancy-page`) - Сторінка вакансій
9. **VideoInterview** (`video-interview`) - Сторінка відео інтерв'ю

### Структура файлів:

```
updated/pages/
├── About/
│   ├── en.json  (Англійська)
│   ├── uk.json  (Українська)
│   ├── pl.json  (Польська)
│   └── ru.json  (Російська)
├── Contact/
│   ├── en.json
│   ├── uk.json
│   ├── pl.json
│   └── ru.json
└── ... (інші сторінки)
```

### Локалі:

- `en` - Англійська
- `uk` - Українська
- `pl` - Польська
- `ru` - Російська

---

## 📝 Сценарій 1: Редагування даних на сторінці

### Крок 1: Визначення сторінки та мови

**Одразу запитай:**

> "Яку сторінку ви хочете редагувати?"
> - About (Про нас)
> - Contact (Контакти)
> - Header (Хедер)
> - Home (Головна)
> - NotFound (404)
> - ThankYou (Подяка)
> - VacancyListData (Дані вакансій)
> - VacancyPage (Сторінка вакансій)
> - VideoInterview (Відео інтерв'ю)
> 
> **Якою мовою?**
> - en (Англійська)
> - uk (Українська)
> - pl (Польська)
> - ru (Російська)

**Після отримання відповіді:**

**AI САМ виконує:**

1. Визначає шлях до файлу: `updated/pages/{PageName}/{locale}.json`
2. Читає файл
3. Показує користувачу структуру даних

**Приклад відповіді:**

> "Відмінно! Файл знаходиться тут:
> 
> `updated/pages/About/en.json`
> 
> Зараз читаю файл і покажу вам структуру даних..."

### Крок 2: Показ структури та запит змін

**Після читання файлу:**

**AI показує структуру:**

> "Ось поточна структура даних:
> 
> ```json
> {
>   "title": "Remote Employees - Outstaffing company",
>   "firstDescription": "We provide jobs...",
>   "menu": [...],
>   "languagesList": [...],
>   ...
> }
> ```
> 
> **⚠️ ВАЖЛИВО:** 
> - Редагуйте дані **НА КОРЕНЕВОМУ РІВНІ** файлу
> - **НЕ** редагуйте поля `createdAt`, `updatedAt`, `locale`, `localizations`
> - Ці поля автоматично обробляються скриптом
> 
> **Що ви хочете змінити?** Надайте назву поля та нове значення."

### Крок 3: Редагування файлу

**Після отримання інформації про зміни:**

**AI САМ виконує:**

1. Читає файл
2. Оновлює вказані поля
3. **НЕ змінює** системні поля (`createdAt`, `updatedAt`, `locale`, `localizations`)
4. Зберігає файл

**Приклад:**

> "Оновлюю файл:
> - `title`: 'Remote Employees' → 'Remote Employees - Updated'
> - `firstDescription`: 'Old description' → 'New description'
> 
> ✅ Файл оновлено та збережено!"

### Крок 4: Запит на відправку

**Одразу запитай:**

> "Чи ви бажаєте одразу відправити зміни на сайт чи продовжити редагування?"
> - Так, відправити на сайт
> - Ні, продовжити редагувати

**Якщо ТАК:**

**AI САМ виконує відправку:**

1. Запитує токен (якщо ще не надано)
2. Переходить в папку скриптів
3. Запускає відповідний скрипт оновлення
4. Показує результат

**Команди, які AI виконує:**

```bash
cd ENTITIES/ASSETS/rem-s/scripts/pages-update
node strapi-{page-name}-update.js --confirm YOUR_TOKEN
```

**Після виконання:**

**Якщо успішно:**
> "✅ Відмінно! Дані оновлено на сайті. Зміни мають відобразитися на сайті протягом кількох секунд.
> 
> **Що було оновлено:**
> - About/en.json: title, firstDescription
> 
> **Перевірте сайт:** https://rem-s.com (або інший URL)"

**Якщо помилка:**
> "❌ Помилка: [деталі помилки]. Перевірте токен та підключення до інтернету."

**Якщо НІ (продовжити редагування):**

**Одразу запитай:**

> "Що ще ви хочете змінити?"

---

## ➕ Сценарій 2: Додавання нових даних

### Крок 1: Визначення сторінки та мови

**Одразу запитай (як у Сценарії 1, Крок 1):**

> "Яку сторінку ви хочете редагувати? Якою мовою?"

### Крок 2: Показ структури та запит нових даних

**Після читання файлу:**

**AI показує структуру та запитає:**

> "Ось поточна структура даних. Які нові поля ви хочете додати?
> 
> **Примітка:** 
> - Нові поля додаються на кореневому рівні файлу
> - Назви полів мають відповідати структурі в Strapi
> - Якщо поле не існує в Strapi, воно буде проігноровано
> 
> **Надайте:**
> - Назву поля
> - Значення поля"

### Крок 3: Додавання даних

**Після отримання інформації:**

**AI САМ виконує:**

1. Читає файл
2. Додає нові поля на кореневому рівні
3. Зберігає файл

**Приклад:**

> "Додаю нові поля:
> - `newField`: 'Нове значення'
> - `anotherField`: 'Інше значення'
> 
> ✅ Поля додано та файл збережено!"

### Крок 4: Запит на відправку

**Як у Сценарії 1, Крок 4**

---

## ➖ Сценарій 3: Видалення даних

### Крок 1: Визначення сторінки та мови

**Одразу запитай (як у Сценарії 1, Крок 1):**

> "Яку сторінку ви хочете редагувати? Якою мовою?"

### Крок 2: Показ структури та запит на видалення

**Після читання файлу:**

**AI показує структуру та запитає:**

> "Ось поточна структура даних. Які поля ви хочете видалити?
> 
> **⚠️ ВАЖЛИВО:** 
> - Видалення поля з файлу **НЕ** видалить його з Strapi
> - Поле залишиться в Strapi зі старим значенням
> - Щоб видалити поле з Strapi, потрібно встановити його значення в `null`
> 
> **Що ви хочете зробити?**
> - Видалити поле з файлу (поле залишиться в Strapi)
> - Встановити поле в `null` (поле буде видалено з Strapi)"

### Крок 3: Видалення або встановлення null

**Після отримання відповіді:**

**Якщо видалити з файлу:**

**AI САМ виконує:**

1. Читає файл
2. Видаляє вказані поля
3. Зберігає файл

**Якщо встановити в null:**

**AI САМ виконує:**

1. Читає файл
2. Встановлює вказані поля в `null`
3. Зберігає файл

**Приклад:**

> "Встановлюю поле `oldField` в `null` для видалення з Strapi.
> 
> ✅ Файл оновлено та збережено!"

### Крок 4: Запит на відправку

**Як у Сценарії 1, Крок 4**

---

## 🚀 Сценарій 4: Оновлення всіх сторінок одразу

### Крок 1: Запит токену

**Одразу запитай:**

> "Для оновлення всіх сторінок потрібен API токен. Надайте ваш токен (якщо ще не надали)."

**Після отримання токену:**

**AI САМ виконує:**

1. Перевіряє наявність токену
2. Переходить в папку скриптів
3. Запускає скрипт `update-all-pages.js`
4. Показує результат

**Команди, які AI виконує:**

```bash
cd ENTITIES/ASSETS/rem-s/scripts/pages-update
node update-all-pages.js --confirm YOUR_TOKEN
```

**Після виконання:**

**Якщо успішно:**
> "✅ Відмінно! Всі сторінки оновлено на сайті.
> 
> **Результат:**
> - ✅ Успішно: 10/10
> - ❌ Помилки: 0/10
> - ⏱️ Час виконання: XX секунд
> 
> **Перевірте сайт:** https://rem-s.com"

**Якщо є помилки:**
> "⚠️ Оновлення завершено з помилками:
> 
> **Результат:**
> - ✅ Успішно: X/10
> - ❌ Помилки: Y/10
> 
> **Помилки:**
> - [список помилок]"

---

## 🔍 Сценарій 5: Перевірка даних на сайті (Dry-run)

### Крок 1: Визначення сторінки

**Одразу запитай:**

> "Яку сторінку ви хочете перевірити?"

### Крок 2: Запуск dry-run

**AI САМ виконує:**

1. Переходить в папку скриптів
2. Запускає скрипт в режимі `--dry-run`
3. Показує, що буде оновлено

**Команди, які AI виконує:**

```bash
cd ENTITIES/ASSETS/rem-s/scripts/pages-update
node strapi-{page-name}-update.js --dry-run
```

**Після виконання:**

> "✅ Dry-run завершено! Ось що буде оновлено:
> 
> **Файли для оновлення:**
> - About/en.json
> - About/uk.json
> - About/pl.json
> - About/ru.json
> 
> **Поля, які будуть відправлені:**
> - title
> - firstDescription
> - ...
> 
> **⚠️ УВАГА:** Це тільки перегляд. Ніякі зміни не були застосовані."

---

## 📚 Детальна інформація про структуру даних

### ⚠️ КРИТИЧНО ВАЖЛИВО: Де редагувати дані

**Дані для оновлення в Strapi ЗАВЖДИ знаходяться на КОРЕНЕВОМУ РІВНІ JSON файлу, а НЕ в `localizations`!**

### ✅ Поля, які можна редагувати (на кореневому рівні):

```json
{
  "title": "...",                    ← РЕДАГУЙТЕ ТУТ
  "firstDescription": "...",        ← РЕДАГУЙТЕ ТУТ
  "menu": [...],                     ← РЕДАГУЙТЕ ТУТ
  "languagesList": [...],           ← РЕДАГУЙТЕ ТУТ
  "partnersSlider": {...},           ← РЕДАГУЙТЕ ТУТ (медіа поле)
  
  // ❌ НЕ РЕДАГУЙТЕ ЦІ ПОЛЯ - вони ігноруються:
  "createdAt": "...",               ← ІГНОРУЄТЬСЯ
  "updatedAt": "...",               ← ІГНОРУЄТЬСЯ
  "publishedAt": "...",             ← ІГНОРУЄТЬСЯ (автоматично оновлюється)
  "locale": "...",                  ← ІГНОРУЄТЬСЯ
  "localizations": { ... }          ← ІГНОРУЄТЬСЯ (це дані для інших локалей)
}
```

### 📝 Приклад правильного редагування:

#### ✅ ПРАВИЛЬНО:
```json
{
  "title": "Remote Employees - НОВИЙ ТЕКСТ",
  "firstDescription": "Новий опис...",
  "menu": [...],
  ...
}
```

#### ❌ НЕПРАВИЛЬНО (не працює):
```json
{
  "title": "Старий текст",
  "localizations": {
    "data": [{
      "attributes": {
        "title": "НОВИЙ ТЕКСТ"  ← ЦЕ НЕ ПРАЦЮЄ!
      }
    }]
  }
}
```

### 🖼️ Особливості роботи з медіа полями

**Медіа поля** (як `partnersSlider`, `mainCat`, `vacancyCat`, `videoCat`) мають особливу структуру:

**В файлі (після експорту):**
```json
{
  "partnersSlider": {
    "data": [
      { "id": 367, "attributes": {...} },
      { "id": 307, "attributes": {...} }
    ]
  }
}
```

**⚠️ ВАЖЛИВО:** Скрипт автоматично нормалізує медіа поля перед відправкою. Вам не потрібно змінювати структуру - просто редагуйте ID або залиште як є.

**Якщо потрібно змінити фото:**
1. В Strapi адмін панелі завантажте нове фото
2. Експортуйте дані (через скрипт експорту)
3. Скопіюйте новий ID з експортованого файлу
4. Вставте його в файл для оновлення

### 📋 Список доступних скриптів оновлення:

1. `strapi-about-update.js` - Оновлення сторінки "Про нас"
2. `strapi-contact-update.js` - Оновлення сторінки "Контакти"
3. `strapi-header-update.js` - Оновлення хедера
4. `strapi-home-update.js` - Оновлення головної сторінки
5. `strapi-not-found-update.js` - Оновлення сторінки 404
6. `strapi-thank-you-update.js` - Оновлення сторінки подяки
7. `strapi-vacancy-list-data-update.js` - Оновлення даних списку вакансій
8. `strapi-vacancy-page-update.js` - Оновлення сторінки вакансій
9. `strapi-video-interview-update.js` - Оновлення сторінки відео інтерв'ю
10. `update-all-pages.js` - Оновлення всіх сторінок одразу

---

## 🔧 Команди для запуску скриптів

### Оновлення однієї сторінки:

```bash
# Dry-run (тільки перегляд, без змін)
node strapi-{page-name}-update.js --dry-run

# Реальне оновлення
node strapi-{page-name}-update.js --confirm YOUR_TOKEN

# З backup перед оновленням
node strapi-{page-name}-update.js --confirm --backup YOUR_TOKEN

# З backup та експортом після оновлення
node strapi-{page-name}-update.js --confirm --backup --export-after YOUR_TOKEN
```

### Оновлення всіх сторінок:

```bash
# Dry-run для всіх сторінок
node update-all-pages.js --dry-run

# Реальне оновлення всіх сторінок
node update-all-pages.js --confirm YOUR_TOKEN

# З backup та експортом
node update-all-pages.js --confirm --backup --export-after YOUR_TOKEN
```

### Флаги:

- `--dry-run` - Тільки перегляд без реальних змін (за замовчуванням, якщо не вказано `--confirm`)
- `--confirm` - Підтвердження для реальних оновлень (обов'язково для змін в Strapi)
- `--backup` - Створення backup (експорт) перед оновленням
- `--export-after` - Запуск експорту після успішного оновлення
- `--force` - Продовжити навіть якщо backup не створено (використовуйте з обережністю!)

---

## ⚠️ Важливі зауваження та обмеження

### 1. Редагування даних

- **ЗАВЖДИ** редагуйте дані на **КОРЕНЕВОМУ РІВНІ** файлу
- **НЕ** редагуйте поля `createdAt`, `updatedAt`, `locale`, `localizations`
- **НЕ** редагуйте структуру медіа полів - скрипт автоматично нормалізує їх
- Після редагування **ЗАВЖДИ зберігайте файл** перед запуском скрипта

### 2. Видалення даних

- Видалення поля з файлу **НЕ** видалить його з Strapi
- Щоб видалити поле з Strapi, встановіть його значення в `null`
- Після встановлення `null` поле буде видалено з Strapi при оновленні

### 3. Додавання нових полів

- Нові поля додаються на кореневому рівні файлу
- Назви полів мають відповідати структурі в Strapi
- Якщо поле не існує в Strapi, воно буде проігноровано

### 4. Backup та експорт

- **⚠️ УВАГА:** Флаг `--backup` експортує дані з Strapi і **перезаписує локальні файли**!
- Якщо ви редагували файли вручну, вони будуть перезаписані даними з Strapi
- Завжди зберігайте свої зміни перед використанням `--backup`
- Флаг `--export-after` також перезаписує файли після оновлення

### 5. Публікація даних

- Скрипт автоматично оновлює `publishedAt` для опублікованих даних
- Це гарантує, що Strapi розпізнає оновлення як нову публікацію
- Дані мають відображатися на сайті після оновлення

### 6. Медіа поля

- Медіа поля автоматично нормалізуються перед відправкою
- Для зміни фото: завантажте нове фото в Strapi, експортуйте дані, скопіюйте новий ID

---

## 🔍 Діагностика проблем

### Проблема: Дані не оновлюються на сайті

**Можливі причини:**

1. **Кешування на фронтенді**
   - Спробуйте очистити кеш браузера
   - Перевірте в режимі інкогніто
   - Перезавантажте сторінку з Ctrl+F5

2. **Кешування в Strapi**
   - Можливо, потрібно перезапустити Strapi
   - Або очистити кеш Strapi

3. **Проблема з публікацією**
   - Перевірте логи - чи оновлюється `publishedAt`
   - Перевірте, чи дані опубліковані в Strapi адмін панелі

**Що робити:**

1. Перевірте логи скрипта - чи є повідомлення "📅 Оновлено publishedAt"
2. Перевірте Strapi адмін панель - чи оновлені дані там
3. Очистіть кеш браузера та перезавантажте сторінку

### Проблема: Помилка при запуску скрипта

**Можливі причини:**

1. **Невірний токен**
   - Перевірте, чи токен правильний
   - Перевірте, чи токен не застарів
   - Перевірте, чи токен має права `Full access`

2. **Проблеми з підключенням**
   - Перевірте підключення до інтернету
   - Перевірте, чи доступний Strapi: https://strapi.rem-s.com

3. **Проблеми з файлами**
   - Перевірте, чи файл існує
   - Перевірте, чи файл має правильний формат JSON
   - Перевірте, чи файл збережено

**Що робити:**

1. Перевірте логи скрипта - там буде детальна інформація про помилку
2. Перевірте токен в Strapi адмін панелі
3. Спробуйте запустити скрипт з `--dry-run` для перевірки

### Проблема: Зміни зникли після запуску скрипта

**Можливі причини:**

1. **Використано `--backup` або `--export-after`**
   - Ці флаги експортують дані з Strapi і перезаписують локальні файли
   - Ваші локальні зміни будуть втрачені

**Що робити:**

1. Не використовуйте `--backup` або `--export-after`, якщо редагували файли вручну
2. Або зберігайте копії файлів перед використанням цих флагів
3. Або спочатку оновіть Strapi, потім експортуйте дані

---

## 📖 Додаткові ресурси

### Детальна документація:

- `scripts/pages-update/EXPLANATION.md` - Детальне пояснення роботи скриптів
- `scripts/pages-update/INSTRUCTIONS.md` - Інструкції по редагуванню файлів
- `scripts/pages-update/README.md` - Загальний README

### Структура проекту:

```
ENTITIES/ASSETS/rem-s/
├── scripts/
│   └── pages-update/          # Скрипти оновлення сторінок
│       ├── strapi-*-update.js # Скрипти для окремих сторінок
│       ├── update-all-pages.js # Скрипт для всіх сторінок
│       ├── strapi-api-retry.js # Модуль з логікою спроб
│       └── logs/              # Логи виконання
├── updated/
│   └── pages/                 # Файли для оновлення
│       ├── About/
│       ├── Contact/
│       └── ... (інші сторінки)
└── guides/
    └── AI_PAGES_WORKFLOW.md   # Цей файл
```

---

## ✅ Чеклист для AI перед початком роботи

**Коли користувач звертається з просьбою про сторінки:**

- [ ] Перевірив наявність Node.js
- [ ] Перевірив наявність API токену
- [ ] Визначив, яку сторінку потрібно редагувати
- [ ] Визначив мову
- [ ] Прочитав файл
- [ ] Показав структуру користувачу
- [ ] Запитав, що потрібно змінити
- [ ] Оновив файл (якщо потрібно)
- [ ] Запитав, чи відправити на сайт
- [ ] Запустив скрипт (якщо потрібно)
- [ ] Показав результат

---

## 🎯 Швидкий старт для AI

**Коли користувач каже: "Оновити дані на сторінці":**

1. **Одразу запитай:** "Яку сторінку? Якою мовою?"
2. **Прочитай файл:** `updated/pages/{PageName}/{locale}.json`
3. **Покажи структуру** та запитай, що змінити
4. **Онови файл** (якщо потрібно)
5. **Запитай:** "Відправити на сайт?"
6. **Запусти скрипт:** `node strapi-{page-name}-update.js --confirm TOKEN`

**Коли користувач каже: "Оновити всі сторінки":**

1. **Одразу запитай:** "Надайте API токен"
2. **Запусти скрипт:** `node update-all-pages.js --confirm TOKEN`
3. **Покажи результат**

---

**Кінець документа**

