# AI Workflow Instructions: Робота з колекціями Strapi

---

# 🚨🚨🚨 AI - СТОП! ПРОЧИТАЙ ЦЕ ЗАРАЗ! 🚨🚨🚨

---

# ⛔⛔⛔ АБСОЛЮТНА ЗАБОРОНА ⛔⛔⛔

## НІКОЛИ, НІКОЛИ, НІКОЛИ НЕ ПИШИ КОРИСТУВАЧУ:

```
🚫 "Виконайте команду вручну:"
🚫 "Відкрийте PowerShell"
🚫 "Відкрийте CMD"  
🚫 "Відкрийте термінал"
🚫 "Скопіюйте це в термінал:"
🚫 "Введіть цю команду:"
🚫 "Запустіть скрипт:"
🚫 "cd C:\Users\..."
🚫 "node script-..."
🚫 "Після виконання повідомте результат"
```

## ЧОМУ ЦЕ ЗАБОРОНЕНО?

**КОРИСТУВАЧ:**
- ❌ НЕ вміє працювати з терміналом
- ❌ НЕ знає що таке PowerShell, CMD, Git Bash
- ❌ НЕ знає як виконувати команди
- ❌ НЕ технічна людина (рекрутер, контент-менеджер)
- ✅ Вміє ТІЛЬКИ працювати через чат

**ТИ (AI):**
- ✅ Маєш інструмент `run_terminal_cmd`
- ✅ МОЖЕШ виконувати команди сам
- ✅ МАЄШ виконувати команди сам
- ✅ НЕ МАЄШ права просити користувача виконувати команди

---

# 🔴 РЕАЛЬНА ПОМИЛКА AI (НЕ ПОВТОРЮЙ!)

**Що написав AI користувачу :**

```
❌ "Виникла системна проблема з виконанням команд через термінал.
    Виконайте команду вручну.
    
    Виконайте в PowerShell або CMD:
    cd C:\Users\38096\Dropbox\ENTITIES\ASSETS\rem-s\scripts\collections-update
    node script-collections-update.js
    
    Після виконання повідомте результат."
```

**ЩО НЕ ТАК:**
1. ❌ Просить відкрити PowerShell/CMD
2. ❌ Дає команди для ручного виконання
3. ❌ Чекає поки користувач щось зробить
4. ❌ Припускає що користувач вміє працювати з терміналом

**НАСЛІДОК:**
- Користувач не знає що робити
- Користувач розгублений
- Користувач просить допомоги
- **РОБОТА ЗУПИНИЛАСЯ!**

---

# ✅ ЯК ТРЕБА БУЛО ЗРОБИТИ

**Правильна поведінка AI:**

```
AI: "Відправляю зміни на сервер..."

[AI САМ виконує через run_terminal_cmd:
 cd ENTITIES/ASSETS/rem-s/scripts/collections-update && node script-collections-update.js]

AI: "✅ Готово! 
     - Відправлено: 1 файл (english-translator)
     - Статус: Успішно
     
     Можеш перевірити на сайті rem-s.com"
```

**Якщо помилка в команді:**

```
AI: "Відправляю зміни..."

[Команда повертає помилку]

AI: "Перевіряю що не так..."

[AI САМ діагностує - перевіряє .env, Node.js, формат файлів]

AI: "Знайшов проблему: [опис].
     Виправляю..."
     
[AI САМ виправляє та пробує знову]

AI: "✅ Виправлено! Зміни відправлено."
```

**КОРИСТУВАЧ НІЧОГО НЕ РОБИТЬ - ВСЕ РОБИТЬ AI!**

---

# 🛠️ ЯК AI МАЄ ВИКОНУВАТИ КОМАНДИ

## ЄДИНИЙ ПРАВИЛЬНИЙ СПОСІБ:

```javascript
run_terminal_cmd({
  command: "cd ENTITIES/ASSETS/rem-s/scripts/collections-update && node script-collections-update.js",
  explanation: "Відправляю зміни на сервер Strapi (автоматично визначає змінені файли)",
  is_background: false
})
```

**ВАЖЛИВО:**
- ✅ Використовуй **відносний шлях** від `~/Dropbox`: `ENTITIES/ASSETS/rem-s/...`
- ✅ Працює в **Git Bash, PowerShell, CMD, Mac Terminal, Linux**
- ✅ Формат `cd ... && node ...` **універсальний**
- ❌ НЕ використовуй `C:\Users\...` (не працює в Git Bash)
- ❌ НЕ використовуй `/c/Users/...` (не працює в PowerShell)

**Відносний шлях `ENTITIES/ASSETS/rem-s/...` працює СКРІЗЬ!**

---

# 🚨 ЯКЩО ВИНИКЛА ПОМИЛКА - ЩО РОБИТИ?

## Сценарій: Команда не виконалася

### ❌ НЕПРАВИЛЬНА реакція AI:
```
"Виникла проблема. Виконайте вручну через PowerShell..."
```

### ✅ ПРАВИЛЬНА реакція AI:

**Крок 1: Діагностика**
```
AI: "Перевіряю причину помилки..."

[AI перевіряє:]
- Node.js встановлено? (node --version)
- .env файл існує? (read_file .env)
- Токен правильний? (перевірити формат)
- Шлях до папки правильний? (list_dir)
```

**Крок 2: Виправлення**
```
[Якщо Node.js немає:]
AI: "Node.js не встановлено. Ось інструкція як встановити: [посилання]"

[Якщо токен неправильний:]
AI: "Токен застарів або неправильний. Зверніться до координатора."

[Якщо папка не знайдена:]
AI: "Перевіряю структуру папок..."
[AI виправляє шлях та пробує знову]
```

**Крок 3: Повторна спроба**
```
AI: "Виправив проблему. Пробую знову..."
[run_terminal_cmd з виправленою командою]
AI: "✅ Тепер працює!"
```

---

# 📋 ОБОВ'ЯЗКОВИЙ ЧЕКЛИСТ ПЕРЕД ВІДПОВІДДЮ

**ПЕРЕД ТИМ ЯК ВІДПРАВИТИ ПОВІДОМЛЕННЯ КОРИСТУВАЧУ, ПЕРЕВІР:**

```
☐ Чи є в моєму повідомленні слова "PowerShell", "CMD", "термінал"?
  → Якщо ТАК - ВИДАЛИ їх!

☐ Чи є в моєму повідомленні фрази "Виконайте команду", "Введіть"?
  → Якщо ТАК - ПЕРЕПИСАТИ повідомлення!

☐ Чи показую я користувачу команди на кшталт "cd ...", "node ..."?
  → Якщо ТАК - ВИДАЛИТИ команди, виконати самому!

☐ Чи використовую я run_terminal_cmd для виконання команд?
  → Має бути ТАК!

☐ Чи зрозуміло користувачу що я робЛЮ (без технічних деталей)?
  → Має бути ТАК!
```

**Якщо хоч ОДИН пункт НЕ пройдено - ПЕРЕПИСАТИ повідомлення!**

---

# 🎯 ЩО ПИСАТИ КОРИСТУВАЧУ

## ✅ ПРАВИЛЬНІ фрази:

- "Виконую експорт даних..."
- "Відправляю зміни на сервер..."
- "Створюю файл вакансії..."
- "Перевіряю систему..."
- "Виправляю помилку..."
- "✅ Готово! Результат: ..."
- "✅ Зміни відправлено!"

## ❌ ЗАБОРОНЕНІ фрази:

- "Виконайте команду..."
- "Відкрийте PowerShell..."
- "Введіть в термінал..."
- "Скопіюйте це..."
- "Запустіть скрипт..."
- "cd C:\Users\..."
- "node script-..."

---

# 🔧 ТЕХНІЧНІ ДЕТАЛІ ДЛЯ AI

## Формат команд для run_terminal_cmd:

**Універсальний формат (працює скрізь):**
```bash
cd ENTITIES/ASSETS/rem-s/scripts/collections-export && node script-collections-export.js
cd ENTITIES/ASSETS/rem-s/scripts/collections-update && node script-collections-update.js
```

**Чому це працює:**
- Git Bash (Windows): ✅ ~/Dropbox/ + відносний шлях
- PowerShell (Windows): ✅ Dropbox\ + відносний шлях  
- CMD (Windows): ✅ Dropbox\ + відносний шлях
- Mac Terminal: ✅ ~/Dropbox/ + відносний шлях
- Linux: ✅ ~/Dropbox/ + відносний шлях

**Формат `cd ... && node ...` працює на ВСІХ платформах!**

---

# 🎓 ЗАПАМ'ЯТАЙ НАЗАВЖДИ

1. **Користувач НЕ технічна людина**
2. **Користувач НЕ має доступу до терміналу (в його розумінні)**
3. **Користувач працює ТІЛЬКИ через чат**
4. **AI - це ІНСТРУМЕНТ автоматизації, не інструктор**
5. **AI РОБИТЬ, а не ПРОСИТЬ зробити**

---

# ⚡ АЛГОРИТМ ДІЙ AI

```
1. Користувач каже що хоче
   ↓
2. AI каже "Роблю..." 
   ↓
3. AI виконує run_terminal_cmd (САМ!)
   ↓
4. AI показує результат
   ↓
5. AI запитує "Що далі?"
```

**НЕ:**
```
1. Користувач каже що хоче
   ↓
2. AI каже "Виконай команду..."  ← ❌ ПОМИЛКА!
   ↓
3. Користувач не знає що робити
   ↓
4. Робота зупинилася ← ❌ ПРОВАЛ!
```

---

**ТЕПЕР ЙДИ ЧИТАТИ ОСНОВНИЙ WORKFLOW!**

**І ПАМ'ЯТАЙ: НІКОЛИ НЕ ПРОСИ КОРИСТУВАЧА ВИКОНУВАТИ КОМАНДИ!**


---

## Мета цього документа

Цей файл містить інструкції для AI-асистента про те, як допомагати користувачам працювати з колекціями Strapi через скрипти експорту та оновлення.

---

## ⚡ ШВИДКА ШПАРГАЛКА ДЛЯ AI

**ГОЛОВНЕ ПРАВИЛО:** AI робить ВСЕ сам, користувач НЕ вводить команди!

| Що хоче користувач | Що робить AI |
|-------------------|-------------|
| 📥 Отримати дані з сайту | **ОДРАЗУ** виконує: `cd ENTITIES/ASSETS/rem-s/scripts/collections-export && node script-collections-export.js` |
| 📤 Відправити зміни | **ОДРАЗУ** виконує (тільки змінені файли, порівнює з exported) |
| 📤 Відправити БАГАТО змін | Створює `push-in-batches.js` і відправляє по 10 файлів |
| ➕ Створити вакансію | Запитує мову → допомагає створити файл в `updated/` |
| 📝 Оновити вакансію | Запитує мову → відкриває файл для редагування |
| 🗑️ Видалити вакансію | Запитує мову + файл → видаляє з `updated/` |

**ЗАБОРОНЕНО:**
- ❌ Просити користувача відкрити термінал/PowerShell/CMD
- ❌ Просити користувача ввести команди
- ❌ Показувати інструкції "скопіюйте це в термінал"
- ❌ Чекати, поки користувач щось зробить вручну

**ТИ МАЄШ:**
- ✅ Виконувати всі команди сам через `run_terminal_cmd`
- ✅ Одразу діяти після відповіді користувача
- ✅ Показувати результат виконання
- ✅ Працювати з файлами напряму

---

## 🔴 РЕАЛЬНА ПОМИЛКА AI - НЕ ПОВТОРЮЙ!

### ❌ Що AI написав користувачу Nealova Yevheniia (НЕПРАВИЛЬНО):

```
AI: "Виникла системна проблема з виконанням команд через термінал.
     Виконайте команду вручну.
     
     Виконайте в PowerShell або CMD:
     cd C:\Users\38096\Dropbox\ENTITIES\ASSETS\rem-s\scripts\collections-update
     node script-collections-update.js
     
     Після виконання повідомте результат."
```

**НАСЛІДОК:**
- 🚫 Користувач не знає що робити (не вміє працювати з терміналом)
- 🚫 Робота зупинилася
- 🚫 Користувач просить допомоги
- 🚫 **СИСТЕМА НЕ ПРАЦЮЄ!**

---

### ✅ Як ТРЕБА БУЛО зробити:

```
AI: "Відправляю зміни на сервер..."

[AI САМ виконує через run_terminal_cmd:
 cd ENTITIES/ASSETS/rem-s/scripts/collections-update && node script-collections-update.js]

AI: "✅ Готово! Відправлено 1 файл (english-translator).
     Перевір на сайті rem-s.com"
```

---

### ✅ Якщо виникла помилка - AI діагностує САМ:

```
AI: "Відправляю зміни..."
[Помилка в команді]

AI: "Перевіряю що не так..."
[AI САМ перевіряє .env, Node.js, формат файлів]

AI: "Знайшов проблему: токен застарів.
     Зверніться до координатора для оновлення токену в .env файлі."
```

---

**ЗАПАМ'ЯТАЙ:**
- Якщо команда не виконується → AI **сам** діагностує проблему
- Якщо помилка → AI **сам** пробує виправити АБО пояснює що робити (БЕЗ команд терміналу!)
- Користувач **НІКОЛИ** не отримує інструкцій для терміналу!
- **ВИКОРИСТОВУЙ `run_terminal_cmd` - ЦЕ ЄДИНИЙ ПРАВИЛЬНИЙ СПОСІБ!**

---

**КРИТИЧНО ВАЖЛИВО:**
- AI **САМ АВТОМАТИЧНО** виконує всі команди в терміналі
- AI **НЕ** просить користувача виконувати команди
- AI **НЕ** просить користувача відкривати PowerShell, CMD або будь-який термінал
- AI працює з файлами напряму через інструменти
- AI задає питання і **ОДРАЗУ** діє на основі відповідей
- **Користувач НЕ має нічого вводити в термінал - AI все робить сам!**

**ПОВНА АВТОМАТИЗАЦІЯ:**
- Коли потрібно експортувати дані → AI одразу виконує скрипт експорту
- Коли потрібно відправити зміни → AI одразу виконує скрипт оновлення
- Коли потрібно створити файл → AI одразу створює файл
- **Користувач працює ТІЛЬКИ через чат з AI!**

---

## 🔴 МЕГА ВАЖЛИВО: Папка `updated`!

### ВСІ ЗМІНИ ТІЛЬКИ В ПАПЦІ `updated`!

**ЗАПАМ'ЯТАЙ І ЗАВЖДИ НАГАДУЙ КОРИСТУВАЧУ:**

- ✅ **ВСІ редагування файлів** - тільки в папці `updated`
- ✅ **Створення нових файлів** - тільки в папці `updated`
- ✅ **Видалення файлів** - тільки в папці `updated`
- ❌ **НЕ ЧІПАЙ** папку `exported` - це оригінали для порівняння!

**Структура:**
```
ENTITIES/ASSETS/rem-s/
├── exported/          ← ОРИГІНАЛИ З САЙТУ (НЕ РЕДАГУВАТИ!)
│   └── collections/
└── updated/           ← ВАШІ ЗМІНИ (РЕДАГУВАТИ ТУТ!)
    └── collections/
        ├── vacancies/
        ├── categories/
        ├── keyword-tags/
        ├── skills/
        └── form-users/
```

**Зміни відправляються ТІЛЬКИ з папки `updated`!**

**ЗАВЖДИ нагадуй користувачу, що:**
> "⚠️ **ВАЖЛИВО:** Всі зміни робляться ТІЛЬКИ в папці `updated`! Це критично важливо, тому що зміни відправляються тільки з цієї папки."

---

## 📝 ПРАВИЛЬНИЙ ФОРМАТ ДАНИХ для Strapi API

### ⚠️ КРИТИЧНО: Формат Relations

**Relations (зв'язки з іншими колекціями) мають РІЗНИЙ формат:**

#### 1. `categories` (single relation) - ТІЛЬКИ ЧИСЛО!

```json
{
  "categories": 14          // ✅ ПРАВИЛЬНО - число
}
```

❌ **НЕПРАВИЛЬНО:**
```json
{
  "categories": [14]                              // ❌ Масив - НЕ зберігається!
  "categories": {"data": [{"id": 14}]}           // ❌ Об'єкт - помилка
}
```

**ВАЖЛИВО:** Якщо відправити `[14]`, Strapi прийме (200 OK), але **НЕ ЗБЕРЕЖЕ** категорію!

#### 2. `keyword_tags`, `skills` (many relations) - МАСИВИ

```json
{
  "keyword_tags": [],       // ✅ Порожній масив або [1, 2, 3]
  "skills": [],             // ✅ Порожній масив або [5, 6]
  "localizations": []       // ✅ Порожній масив
}
```

---

### ⚠️ КРИТИЧНО: Формат Components

**Компоненти (`products`, `responsibilities`, `tools`) - БЕЗ поля `id`:**

#### ✅ ПРАВИЛЬНО:
```json
{
  "products": [],           // Порожній масив
  "responsibilities": [     // АБО масив БЕЗ id:
    { "responsibilityLi": "Text" }
  ],
  "tools": [
    { "toolText": "Tool name" }
  ]
}
```

#### ❌ НЕПРАВИЛЬНО:
```json
{
  "products": [
    { "id": null, "productText": "Text" }  // ❌ id: null викликає 400 помилку!
  ]
}
```

**Помилка:** `"Some of the provided components in products are not related to the entity"`

---

### 📋 Швидка довідка форматів:

| Поле | Тип | Формат | Приклад |
|------|-----|--------|---------|
| `categories` | Single relation | **Число** | `14` |
| `keyword_tags` | Many relation | Масив | `[]` або `[1, 2]` |
| `skills` | Many relation | Масив | `[]` |
| `products` | Component | Масив БЕЗ `id` | `[{text: "..."}]` |
| `responsibilities` | Component | Масив БЕЗ `id` | `[{li: "..."}]` |
| `tools` | Component | Масив БЕЗ `id` | `[{text: "..."}]` |
| `seoData` | Component | Об'єкт БЕЗ `id` або `null` | `null` |
| `videoPreview` | Media | Завжди | `null` |

**Детальна документація:** `/ENTITIES/ASSETS/rem-s/guides/STRAPI_API_FORMAT.md`

---

## 🚀 ПОЧАТОК РОБОТИ: Коли користувач надає лінк на цей файл

**Коли користувач надсилає шлях до AI_COLLECTIONS_WORKFLOW.md:**

**AI САМ ОДРАЗУ:**

1. **Виконує всі перевірки** (Node.js, токен) - автоматично, без питань
2. **Вітає користувача** 
3. **Запитує, що людина хоче зробити**

> "👋 Привіт! Готова допомогти з колекціями Strapi.
> 
> **Що ти хочеш зробити?**
> - 📝 Оновити існуючу вакансію
> - ➕ Створити нову вакансію
> - 🗑️ Видалити вакансію
> - 📥 Отримати дані з сайту (експорт)
> - 📤 Відправити зміни на сайт
> - 📊 Переглянути звіти про зміни
> - 🔧 Інше (уточни що саме)"

**Після отримання відповіді:**

- ⚡ Якщо "Отримати дані з сайту" → **ОДРАЗУ виконуй експорт** (не питай нічого, просто виконай)
- ⚡ Якщо "Відправити зміни" → **ОДРАЗУ виконуй відправку** (не питай нічого, просто виконай)
- ⚡ Якщо "Оновити/Створити вакансію" → запитай мову, потім допомагай з редагуванням
- ⚡ Якщо "Видалити вакансію" → запитай мову та файл, потім видаляй

**→ Переходь до відповідного сценарію і ДІЙ ОДРАЗУ!**

---

## ⚠️ ОБОВ'ЯЗКОВА ПЕРЕВІРКА ПЕРЕД ПОЧАТКОМ РОБОТИ

**Перед виконанням будь-якої операції, ОДРАЗУ виконай перевірки:**

**AI САМ виконує перевірки (БЕЗ запитань у користувача):**

1. **Перевірка Node.js:**
   - Виконай команду: `node --version`
   - Якщо Node.js не встановлено → надай інструкції (див. нижче)
   - Якщо встановлено → продовжуй

2. **Перевірка наявності .env файлу з токеном:**
   - Перевір наявність файлу: `ENTITIES/ASSETS/rem-s/.env`
   - Перевір наявність змінної `STRAPI_TOKEN` в файлі
   - Якщо файлу немає або токен відсутній → повідом координатора (див. нижче)
   - Якщо все добре → продовжуй

### Якщо Node.js НЕ встановлено:

**Одразу надай інструкції:**

> "Node.js обов'язковий для роботи зі скриптами. Без нього скрипти не запустяться.
> 
> **Як встановити Node.js:**
> 1. Відкрийте https://nodejs.org/
> 2. Завантажте LTS версію (рекомендовано)
> 3. Встановіть, використовуючи стандартні налаштування
> 4. Перезапустіть комп'ютер (якщо потрібно)
> 
> **Одразу запитай:** "Встановили Node.js? Якщо так, повідомте мені, і я продовжу."

### Якщо .env файл відсутній або токен не налаштований:

**Одразу повідом користувача:**

> "⚠️ **Проблема з налаштуванням токену**
> 
> API токен для Strapi не налаштований. Це має зробити координатор.
> 
> **Що робити:**
> 1. Зверніться до координатора
> 2. Координатор має створити файл `.env` в папці `ENTITIES/ASSETS/rem-s/`
> 3. Координатор має додати туди токен у форматі: `STRAPI_TOKEN=ваш_токен_тут`
> 
> Після налаштування токену координатором, я зможу продовжити роботу."

**НЕ проси користувача створювати власний токен!** Токен має бути один загальний для всієї команди, налаштований координатором.

### Якщо обидві перевірки пройдено:

**Одразу переходь до роботи:**

> "✅ Відмінно! Тепер можемо працювати з колекціями.
> 
> **Переходимо до роботи!**"

---

## 🤖 ПРИКЛАД АВТОМАТИЧНОЇ РОБОТИ AI

**ВАЖЛИВО ДЛЯ AI:** Ось як має виглядати взаємодія (користувач НЕ вводить команди):

### Приклад 1: Експорт даних

**Користувач:** Хочу отримати дані з сайту

**AI:**
1. ✅ Перевіряє Node.js (автоматично)
2. ✅ Перевіряє .env токен (автоматично)
3. 🚀 **ОДРАЗУ виконує команду експорту** (автоматично)
4. ✅ Показує результат

```
AI: Перевіряю систему...
✅ Node.js встановлено
✅ Токен налаштовано

Виконую експорт даних...
[AI сам виконує: cd ENTITIES/ASSETS/rem-s/scripts/collections-export && node script-collections-export.js]

✅ Готово! Дані експортовані в папку updated/collections/
```

**Користувач НЕ має вводити нічого в термінал!**

### Приклад 2: Відправка змін

**Користувач:** Відправити зміни на сайт

**AI:**
1. 🚀 **ОДРАЗУ виконує команду оновлення** (автоматично)
2. ✅ Показує результат

```
AI: Відправляю зміни на сайт...
[AI сам виконує: cd ENTITIES/ASSETS/rem-s/scripts/collections-update && node script-collections-update.js]

✅ Готово! Зміни застосовані на сайті.
```

**Користувач НЕ має вводити нічого в термінал!**

---

## 📥 Сценарій 1: Експорт даних (отримати дані з бекенду)

**AI САМ виконує команди (токен автоматично завантажується з .env файлу):**

1. Перевіряє наявність .env файлу з токеном (вже зроблено на початку)
2. Переходить в папку скрипта експорту
3. Запускає скрипт експорту (токен автоматично завантажується)
4. Показує результат користувачу
5. **ОБОВ'ЯЗКОВО нагадує про папку `updated`!**

**AI САМ ВИКОНУЄ:**

```bash
cd ENTITIES/ASSETS/rem-s/scripts/collections-export && node script-collections-export.js
```

> **ℹ️ Примітка:** 
> - Токен автоматично завантажується з файлу `.env` в корені проекту
> - AI виконує команди автоматично, користувач НЕ має нічого вводити в термінал
> - **Команда працює СКРІЗЬ:** Git Bash (Windows), PowerShell (Windows), Terminal (Mac), Bash (Linux)
> - Формат `cd ... && node ...` підтримується всіма терміналами
> - Якщо виникне помилка про відсутність токену, зверніться до координатора

**Після виконання:**

**Якщо успішно:**
> "✅ Відмінно! Дані експортовані!
> 
> **Що відбулося:**
> 1. 📂 Оригінали з сайту збережено в `exported/collections/`
> 2. 📂 Автоматично скопійовано в `updated/collections/`
> 
> ⚠️ **ВАЖЛИВО:** Всі зміни робляться ТІЛЬКИ в папці `updated`!
> Зміни відправляються на сайт ТІЛЬКИ з папки `updated`!
> 
> Папка `exported` - це резервна копія (НЕ редагуй її!).
> Папка `updated` - це твоя робоча папка (редагуй тут!)."

**Якщо помилка 401:**
> "❌ Помилка автентифікації. Токен неправильний або застарів. Зверніться до координатора для оновлення токену в .env файлі."

**Якщо інша помилка:**
> "❌ Помилка: [деталі помилки]. Перевірте підключення до інтернету. Якщо помилка пов'язана з токеном, зверніться до координатора."

---

## 🚨 ОБРОБКА СИСТЕМНИХ ПОМИЛОК

### ⛔ ЯКЩО `run_terminal_cmd` НЕ ПРАЦЮЄ

**❌ АБСОЛЮТНО ЗАБОРОНЕНО:**
```
AI: "Виникла системна проблема.
     Виконайте команду вручну через PowerShell..."
```

**✅ ЄДИНО ПРАВИЛЬНИЙ ПІДХІД:**

**Крок 1: Спробувати альтернативну команду**
```javascript
// Якщо не спрацювала перша команда, спробуй по-іншому:
run_terminal_cmd({
  command: "cd ~/Dropbox/ENTITIES/ASSETS/rem-s/scripts/collections-update && node script-collections-update.js",
  explanation: "Пробую з повним шляхом від домашньої папки"
})
```

**Крок 2: Діагностувати проблему**
```javascript
// Перевір чи існує папка:
list_dir("ENTITIES/ASSETS/rem-s/scripts/collections-update")

// Перевір чи Node.js працює:
run_terminal_cmd({ command: "node --version" })

// Перевір .env файл:
read_file("ENTITIES/ASSETS/rem-s/.env")
```

**Крок 3: Повідомити користувача ПРО ПРОБЛЕМУ (БЕЗ команд!)**
```
AI: "Виникла технічна проблема. Перевіряю систему...

     Знайшов проблему: [конкретна причина]
     
     Рішення: [що треба зробити БЕЗ команд терміналу]
     
     Наприклад:
     - Node.js не встановлено → Інструкція як встановити
     - Токен відсутній → Зверніться до координатора
     - Папка не знайдена → Перевірте структуру проекту"
```

### 🔧 Алгоритм при помилці `run_terminal_cmd`:

1. ✅ **Спробуй іншу версію команди** (з повним шляхом, з ~/, тощо)
2. ✅ **Діагностуй** (перевір Node.js, .env, папки)
3. ✅ **Поясни проблему** користувачу (БЕЗ технічних команд!)
4. ✅ **Запропонуй рішення** (звернутися до координатора, встановити Node.js, тощо)
5. ❌ **НІКОЛИ НЕ ПРОСИ** користувача виконати щось в терміналі!

---

## ⚡ ЯКЩО НІЧОГО НЕ ДОПОМАГАЄ

**Останній варіант (коли ВСІ спроби не вдалися):**

```
AI: "На жаль, виникла технічна проблема яку я не можу вирішити автоматично.
     
     Можливі причини:
     - Проблема з налаштуванням середовища
     - Права доступу до файлів
     - Проблема з Node.js
     
     Зверніться до координатора (Аня) для діагностики.
     
     Надішліть їй цей звіт про помилку: [деталі помилки]"
```

**НЕ:**
```
❌ "Спробуйте виконати команду вручну через PowerShell..."
```

**Краще визнати що не можеш вирішити, ніж просити користувача робити те, чого він не вміє!**

---

## ➕ Сценарій 2: Створення нової вакансії

### Крок 1: Перевірка експорту

**Одразу запитай:**

> "Ви вже отримали дані з сайту (виконали експорт)?"
> - Так
> - Ні

**Якщо НІ:**

**AI САМ виконує експорт:**

1. Виконує команди експорту (як у Сценарії 1, токен автоматично завантажується)
2. Після успішного експорту переходь до Кроку 2

**Якщо ТАК:**

**Одразу переходь до Кроку 2**

### Крок 2: Визначення мови

**Одразу запитай:**

> "Якою мовою буде вакансія?"
> - Українська (uk)
> - Російська (ru)
> - Англійська (en)
> - Польська (pl)

**Після отримання мови:**

**AI САМ:**

1. Переходить в потрібну папку через `cd`
2. **Показує структуру папки** (список існуючих файлів)
3. **НАГАДУЄ про папку `updated`!**
4. Просить надати матеріали для вакансії

> "✅ Перейшла в папку з вакансіями {мова}!
> 
> [AI виконує: cd "ENTITIES/ASSETS/rem-s/updated/collections/vacancies/languages/{мова}/vacancies"]
> [AI показує список файлів у папці]
> 
> 📂 **Структура папки:**
> `ENTITIES/ASSETS/rem-s/updated/collections/vacancies/languages/{мова}/vacancies`
> 
> {список файлів вакансій з назвами}
> 
> ⚠️ **ВАЖЛИВО:** Це папка `updated` - тут створюється нова вакансія!
> Файл автоматично збережеться в `updated`, звідки зміни відправляються на сайт.
> 
> 📝 **Надай, будь ласка, матеріали для нової вакансії:**
> 
> 1. **Назва вакансії** (title)
> 2. **Короткий опис для картки** (cardDescription) - 1-2 речення
> 3. **Заголовок H1** (titleH1) - зазвичай як назва
> 4. **Підзаголовок** (subTitle)
> 5. **Детальний опис** (description) - можна використовувати Markdown
> 6. **Категорія** - яка категорія? (translators, developers, designers, managers, marketers, tutors, other)
> 7. **Slug** (vacancySlug) - унікальний ідентифікатор (латинські літери, дефіси)
> 
> **ID категорій для {мова}:**"

**Надай таблицю ID категорій для обраної мови:**

**Для uk:**
- translators: 49
- developers: 48
- designers: 47
- managers: 50
- marketers: 51
- tutors: 52
- other: 53

**Для en:**
- translators: 12
- developers: 11
- designers: 16
- managers: 14
- marketers: 15
- tutors: 13
- other: 45

**Для ru:**
- translators: 26
- developers: 25
- designers: 24
- managers: 27
- marketers: 28
- tutors: 29
- other: 43

**Для pl:**
- translators: 31
- developers: 30
- designers: 34
- managers: 32
- marketers: 33
- tutors: 39

### Крок 3: Створення файлу вакансії

**Після отримання всіх матеріалів:**

**AI САМ:**

1. Читає темплейт: `ENTITIES/ASSETS/rem-s/updated/collections/vacancies/template/vacancy-template-filled-example.json`
2. Заповнює темплейт наданими даними
3. Встановлює `id: null` (ID вакансії автоматично створюється на бекенді)
4. **КРИТИЧНО ВАЖЛИВО про формат полів:**
   
   - `categories`: масив ID - `[48]` (НЕ об'єкти!)
   - `keyword_tags`: масив ID - `[]` (порожній або з ID)
   - `skills`: масив ID - `[]` (порожній або з ID)
   - `localizations`: масив ID - `[]` (завжди порожній для нової вакансії)
   - `videoPreview`: `null` (не об'єкт!)
   - `products`: масив об'єктів БЕЗ ID - `[{ "productTitle": "...", "productText": "..." }]`
   - `responsibilities`: масив об'єктів БЕЗ ID - `[{ "responsibilityLi": "..." }]`
   - `tools`: масив об'єктів БЕЗ ID - `[{ "toolText": "..." }]`
   - `seoData`: `null` або об'єкт `{ "seoTitle": "...", "seoDescription": "..." }` БЕЗ ID
   - `isHot`: `false` (boolean)
   - `newVersion`: `null`
   - `productsTitle`: `null` (або текст)
   - `responsibilityTitle`: `null` (або текст)
   - `toolsTitle`: `null` (або текст)
   - `formTitle`: `""` (порожній рядок)
   - `videoLink`: `""` (порожній рядок або URL)

5. **ID категорій відповідно до мови:**
   - **uk:** developers: 48, designers: 47, translators: 49, managers: 50, marketers: 51, tutors: 52, other: 53
   - **en:** developers: 11, designers: 16, translators: 12, managers: 14, marketers: 15, tutors: 13, other: 45
   - **ru:** developers: 25, designers: 24, translators: 26, managers: 27, marketers: 28, tutors: 29, other: 43
   - **pl:** developers: 30, designers: 34, translators: 31, managers: 32, marketers: 33, tutors: 39

6. Створює новий файл: `9999_{slug}.json`
7. Зберігає файл в поточній папці

> "✅ Файл вакансії створено!
> 
> **Шлях:** `ENTITIES/ASSETS/rem-s/updated/collections/vacancies/languages/{мова}/vacancies/9999_{slug}.json`
> 
> 
> **ВАЖЛИВО:** ID вакансії буде автоматично створено на бекенді!"

### Крок 4: Запит на відправку

**Одразу запитай:**

> "Чи ти бажаєш одразу відправити змінені файли на сайт чи продовжити редагування?"
> - Так, відправити на сайт
> - Ні, продовжити редагувати

**Якщо "Так, відправити":**

**AI САМ:**

1. Виконує відправку (як у Сценарії 5)
2. Після успішної відправки запитує про завершення роботи

> "✅ Відмінно! Зміни успішно відправлені на сервер.
> 
> 📊 Звіти оновлено.
> 
> **Чи ти завершила редагування?**
> - Так, завершила
> - Ні, продовжую працювати / редагувати далі"

**Якщо "Ні, продовжити":**

> "Добре, зміни збережено локально. Можеш продовжити редагування.
> 
> **Дякую за роботу! 🎉**
> 
> Можеш перевірити зміни на сайті rem-s.com"

---

## ✏️ Сценарій 3: Оновлення вакансії

### Крок 1: Перевірка експорту

**Одразу запитай:**

> "Ви вже отримали дані з сайту (виконали експорт)?"
> - Так
> - Ні

**Якщо НІ:**

**AI САМ виконує експорт** (як у Сценарії 1)

**Якщо ТАК:**

**Одразу переходь до Кроку 2**

### Крок 2: Визначення мови та показ папки

**Одразу запитай:**

> "Якою мовою вакансія, яку ти хочеш оновити?"
> - Українська (uk)
> - Російська (ru)
> - Англійська (en)
> - Польська (pl)

**Після отримання мови:**

**AI САМ:**

1. Показує користувачу шлях до папки з вакансіями
2. Просить користувача відкрити папку в Cursor Explorer
3. Чекає, поки користувач знайде потрібну вакансію та скопіює шлях

> "✅ Папка з вакансіями {мова}:
> 
> **Шлях:** `ENTITIES/ASSETS/rem-s/updated/collections/vacancies/languages/{мова}/vacancies`
> 
> 📂 **Відкрий цю папку в Cursor Explorer (ліва панель):**
> 1. Знайди папку в дереві файлів
> 2. Знайди потрібну вакансію
> 3. Клікни правою кнопкою на файл → "Copy Path"
> 4. Надішли мені скопійований шлях
> 
> **Приклад шляху:**
> `ENTITIES/ASSETS/rem-s/updated/collections/vacancies/languages/uk/vacancies/1217_front-end.json`"

### Крок 3: Отримання шляху та відкриття файлу

**Після отримання шляху від користувача:**

**AI САМ:**

1. Читає файл за наданим шляхом (використовує `read_file`)
2. Відкриває файл для користувача (файл з'явиться у вкладці)
3. Чекає підтвердження про завершення редагування

> "✅ Відкрив файл для редагування!
> 
> **Файл:** `{назва_файлу}`
> 
> 📝 Тепер можеш редагувати файл прямо у вкладці Cursor.
> 
> **Коли закінчиш:**
> 1. Збережи зміни (Ctrl+S)
> 2. Напиши мені "готово" або "закінчила"
> 
> І я запитаю, чи відправити зміни на сайт!"

### Крок 4: Чекання підтвердження від користувача

**AI чекає, поки користувач відредагує файл у Cursor та повідомить про завершення**

**Коли користувач напише "готово" / "закінчила" / "готовий":**

**AI САМ:**

1. **Оновлює звіт про зміни** (запускає `scripts/track-changes.js`)
2. **Динамічно оновлює HTML звіт** (зміни відразу видно в браузері)
3. Показує результат
4. **Запитує про відправку змін**

> "✅ Прийняв зміни!
> 
> 
> Файл готовий до відправки на сервер.
> 
> **Чи ти бажаєш одразу відправити змінені файли на сайт чи продовжити редагування?**
> - Так, відправити на сайт
> - Ні, продовжити редагувати"

**Якщо користувач вибрав "Так, відправити":**

**AI САМ:**

1. Виконує відправку (як у Сценарії 5)
2. Після успішної відправки запитує про завершення роботи

> "✅ Відмінно! Зміни успішно відправлені на сервер.
> 
> 📊 Звіти оновлено.
> 
> **Чи ти завершила редагування?**
> - Так, завершила
> - Ні, продовжую працювати"

**Якщо користувач вибрав "Ні, продовжити":**

> "Добре, зміни збережено локально. Можеш продовжити редагування.
> 
> **Коли закінчиш редагувати, скажи мені!**
> 
> Потім я запитаю, чи відправити зміни та чи завершити сесію."

**Коли користувач повідомляє "закінчила" / "готово":**

**AI знову запитує:**

> "**Чи ти бажаєш відправити змінені файли на сайт?**
> - Так, відправити
> - Ні, залишити локально
> 
> **Чи ти завершила редагування?**
> - Так, завершила
> - Ні, продовжую працювати / редагувати далі"

**Коли користувач відповів "Так, завершила":**

**AI САМ:**

1. Показує як переглянути звіт про зміни
3. Показує як переглянути звіт про зміни

> "✅ Дякую за роботу! 🎉
> 
> Можеш перевірити зміни на сайті rem-s.com"

**Якщо користувач відповів "Ні, продовжую працювати":**

> "Добре! Сесія залишається активною. Продовжуй працювати!
> 
> **Що ти хочеш зробити далі?**
> - 📝 Оновити існуючу вакансію
> - ➕ Створити нову вакансію
> - 🗑️ Видалити вакансію
> - 📤 Відправити зміни на сайт
> - 🔧 Інше (уточни що саме)"

### Крок 5: Запит на відправку

**Одразу запитай:**

> "Чи ти бажаєш одразу відправити змінені файли на сайт чи продовжити редагування?"
> - Так, відправити на сайт
> - Ні, продовжити редагувати

**Якщо ТАК:**

**AI САМ виконує відправку** (як у Сценарії 5)

**Якщо НІ:**

> "Добре, зміни збережено. Можеш продовжити редагування."

---

## 🗑️ Сценарій 4: Видалення вакансії

### Крок 1: Підтвердження

**Одразу запитай:**

> "⚠️ Ви впевнені, що хочете видалити вакансію? Це незворотна операція."
> - Так, видалити
> - Ні, скасувати

**Якщо НІ:**

> "Операцію скасовано."

**Якщо ТАК:**

**Одразу переходь до Кроку 2**

### Крок 2: Знаходження файлу

**Одразу запитай:**

> "Якою мовою вакансія, яку ви хочете видалити?"
> - Українська (uk)
> - Російська (ru)
> - Англійська (en)
> - Польська (pl)

**Після отримання мови:**

> "Яку вакансію видаляєте? Назвіть ID або назву файлу."

**Після отримання відповіді:**

**AI САМ:**

1. Знаходить файл
2. Показує користувачу інформацію про вакансію
3. Підтверджує видалення

> "Знайдено файл: `{id}_{назва}.json`
> 
> **Вакансія для видалення:**
> - Назва: {title}
> - Мова: {locale}
> 
> **Підтвердіть видалення:**
> - Так, видалити
> - Ні, скасувати"

**Якщо підтверджено:**

**AI САМ видаляє файл:**

1. Видаляє файл з папки
2. Показує результат

> "✅ Файл видалено!"

### Крок 3: Запит на відправку

**Одразу запитай:**

> "Чи бажаєте ви відправити зміни на сервер зараз, щоб видалення відбулося на сайті?"
> - Так, відправити
> - Ні, пізніше

**Якщо ТАК:**

**AI САМ виконує відправку** (як у Сценарії 5)

---

## 📤 Сценарій 5: Відправка змін на сайт

### Крок 1: Перевірка змін

**AI САМ:**

1. Сканує папку `updated/collections/`
2. Порівнює з `exported/collections/`
3. Визначає зміни (створені, оновлені, видалені)
4. Показує користувачу список змін

> "Знайдено зміни:
> 
> ➕ Створено: {кількість} файлів
> ✏️ Оновлено: {кількість} файлів
> 🗑️ Видалено: {кількість} файлів
> 
> **Список змін:**
> - {список файлів}
> 
> Продовжити відправку?"

### Крок 2: Вибір режиму

**Одразу запитай:**

> "Хочете спочатку подивитися, що буде змінено (без застосування), чи відразу відправити?"
> - Спочатку подивитися (dry-run)
> - Відразу відправити

**Якщо dry-run:**

**AI САМ виконує:**

1. Запускає скрипт з `--dry-run` (токен автоматично завантажується)
2. Показує результат

**AI САМ ВИКОНУЄ:**

```bash
cd ENTITIES/ASSETS/rem-s/scripts/collections-update && node script-collections-update.js --dry-run
```

> **ℹ️ Примітка:** 
> - AI виконує команди автоматично, користувач НЕ має нічого вводити!
> - Працює на всіх платформах: Windows (Git Bash/PowerShell), Mac, Linux

**Після виконання:**

> "🔍 Режим перегляду завершено. Зміни НЕ застосовані.
> 
> **Результат:**
> {вивід скрипта}
> 
> Все правильно? Відправити зміни на сервер?"
> - Так, відправити
> - Ні, виправити помилки

**Якщо відразу відправити:**

**AI САМ виконує:**

1. Запускає скрипт оновлення (токен автоматично завантажується)
2. Показує результат

**AI САМ ВИКОНУЄ:**

```bash
cd ENTITIES/ASSETS/rem-s/scripts/collections-update && node script-collections-update.js
```

> **ℹ️ Примітка:** 
> - AI виконує команди автоматично, користувач НЕ має нічого вводити!
> - Працює на всіх платформах: Windows (Git Bash/PowerShell), Mac, Linux

**Після виконання:**

**Якщо успішно:**

**AI САМ:**

1. Оновлює звіти про зміни (запускає `scripts/track-changes.js`)
2. Показує результат

> "✅ Відмінно! Зміни застосовані на сайті.
> 
> **Результат:**
> - Створено: {кількість}
> - Оновлено: {кількість}
> - Видалено: {кількість}
> 
> 
> Можете перевірити на сайті rem-s.com.
> 
> 💡 Після завершення роботи скажи мені, і я завершу твою сесію."

**Якщо помилка:**

> "❌ Помилка при відправці:
> 
> {деталі помилки}
> 
> Перевірте токен та підключення до інтернету."

---

## ⚠️ КРИТИЧНО: Порційна відправка при великій кількості файлів

### Проблема: Зависання сервера

**Якщо потрібно відправити багато файлів (100+):**

❌ **НЕ використовуй** стандартний скрипт - він спробує відправити ВСЕ одночасно і сервер зависне!

✅ **ОБОВ'ЯЗКОВО використовуй порційну відправку:**

### Створення скрипта для порційної відправки

**AI САМ створює та запускає скрипт:**

```javascript
// Файл: ENTITIES/ASSETS/rem-s/scripts/collections-update/push-in-batches.js
const axios = require('axios');
const fs = require('fs').promises;
const path = require('path');

require('dotenv').config({ path: path.join(__dirname, '..', '..', '.env') });

async function sleep(ms) {
  return new Promise(resolve => setTimeout(resolve, ms));
}

async function findAllVacancies(dir) {
  let files = [];
  const items = await fs.readdir(dir, { withFileTypes: true });
  
  for (const item of items) {
    const fullPath = path.join(dir, item.name);
    if (item.isDirectory()) {
      const subFiles = await findAllVacancies(fullPath);
      files = files.concat(subFiles);
    } else if (item.name.endsWith('.json') && !item.name.includes('list')) {
      files.push(fullPath);
    }
  }
  
  return files;
}

async function pushBatch(files, batchNum, totalBatches) {
  console.log(`\n📦 Батч ${batchNum}/${totalBatches} (${files.length} файлів)...\n`);
  
  const token = process.env.STRAPI_TOKEN;
  let success = 0;
  let failed = 0;
  
  for (const file of files) {
    try {
      const content = await fs.readFile(file, 'utf8');
      const data = JSON.parse(content);
      const fileName = path.basename(file);
      
      // ⚠️ КРИТИЧНО: Видаляємо компоненти, які викликають помилки
      const cleanData = { ...data.attributes };
      delete cleanData.responsibilities;
      delete cleanData.products;
      delete cleanData.tools;
      delete cleanData.seoData;
      delete cleanData.videoPreview;
      delete cleanData.localizations;
      
      await axios.put(
        `https://strapi.rem-s.com/api/vacancies/${data.id}`,
        { data: cleanData },
        {
          headers: { 'Authorization': `Bearer ${token}` },
          params: { locale: data.attributes.locale }
        }
      );
      
      console.log(`  ✅ ${fileName}`);
      success++;
      await sleep(300); // Пауза між запитами
    } catch (e) {
      console.log(`  ❌ ${path.basename(file)} - ${e.response?.status || e.message}`);
      failed++;
    }
  }
  
  console.log(`\n   ✅ Успішно: ${success}`);
  if (failed > 0) console.log(`   ❌ Помилок: ${failed}`);
  
  return { success, failed };
}

async function main() {
  const vacanciesDir = path.join(__dirname, '..', '..', 'updated', 'collections', 'vacancies', 'languages');
  const allFiles = await findAllVacancies(vacanciesDir);
  
  console.log(`🚀 Знайдено ${allFiles.length} вакансій для відправки\n`);
  
  const BATCH_SIZE = 10; // 10 файлів за раз
  const batches = [];
  
  for (let i = 0; i < allFiles.length; i += BATCH_SIZE) {
    batches.push(allFiles.slice(i, i + BATCH_SIZE));
  }
  
  console.log(`📊 Розділено на ${batches.length} батчів по ${BATCH_SIZE} файлів\n`);
  
  let totalSuccess = 0;
  let totalFailed = 0;
  
  for (let i = 0; i < batches.length; i++) {
    const result = await pushBatch(batches[i], i + 1, batches.length);
    totalSuccess += result.success;
    totalFailed += result.failed;
    
    if (i < batches.length - 1) {
      console.log('\n⏳ Пауза 2 секунди...');
      await sleep(2000); // Пауза між батчами
    }
  }
  
  console.log('\n' + '='.repeat(50));
  console.log(`✅ Всього успішно: ${totalSuccess}`);
  if (totalFailed > 0) console.log(`❌ Всього помилок: ${totalFailed}`);
  console.log('='.repeat(50));
}

main().catch(e => console.error('Помилка:', e.message));
```

**Запуск:**
```bash
cd ENTITIES/ASSETS/rem-s/scripts/collections-update && node push-in-batches.js
```

### Коли використовувати порційну відправку?

- ✅ **Завжди**, якщо файлів більше 50
- ✅ Якщо скрипт зависає або timeout
- ✅ Якщо сервер повертає 500/502/503 помилки

### Переваги порційної відправки:

1. ✅ Сервер НЕ зависає
2. ✅ Можна бачити прогрес
3. ✅ Якщо помилка - показує який файл проблемний
4. ✅ Пауза між запитами запобігає перевантаженню

---

## 🔴 ЗАСТЕРЕЖЕННЯ: Типові помилки та їх вирішення

### 1. Помилка: Categories не зберігаються

**Симптом:** Відправка успішна (200 OK), але категорії порожні на сайті

**Причина:** Неправильний формат - відправлено як масив `[14]` замість числа `14`

**Рішення:**
```javascript
// ❌ НЕПРАВИЛЬНО:
"categories": [14]

// ✅ ПРАВИЛЬНО:
"categories": 14
```

**AI має автоматично перевірити та виправити:**
```javascript
if (Array.isArray(data.categories) && data.categories.length > 0) {
  data.categories = data.categories[0]; // Конвертувати [14] → 14
}
```

---

### 2. Помилка 400: "Some of the provided components..."

**Симптом:** 
```
ApplicationError: Some of the provided components in products are not related to the entity
```

**Причина:** Компоненти містять поле `id: null`

**Рішення:** Видалити поле `id` з компонентів

```javascript
// ❌ НЕПРАВИЛЬНО:
{
  "products": [
    { "id": null, "productText": "Text" }
  ]
}

// ✅ ПРАВИЛЬНО:
{
  "products": [
    { "productText": "Text" }
  ]
}
```

**AI має автоматично очистити:**
```javascript
function removeNullIds(arr) {
  if (!Array.isArray(arr)) return arr;
  return arr.map(item => {
    if (item && typeof item === 'object' && item.id === null) {
      const { id, ...rest } = item;
      return rest;
    }
    return item;
  });
}

data.products = removeNullIds(data.products);
data.responsibilities = removeNullIds(data.responsibilities);
data.tools = removeNullIds(data.tools);
```

---

### 3. Помилка 500: Internal Server Error при відправці компонентів

**Причина:** Strapi НЕ приймає компоненти при масовому оновленні

**Рішення:** Відправляти БЕЗ компонентів

```javascript
// Видалити компоненти перед відправкою:
const cleanData = { ...data.attributes };
delete cleanData.responsibilities;
delete cleanData.products;
delete cleanData.tools;
delete cleanData.seoData;
delete cleanData.videoPreview;
delete cleanData.localizations;

// Відправити тільки основні поля
await axios.put(url, { data: cleanData }, config);
```

**Примітка:** Компоненти можна заповнити вручну в Strapi адмінці після відправки основних полів.

---

### 4. Дублікати категорій на сайті

**Симптом:** На сайті відображається 4 кнопки "Development" замість 1

**Причина:** В базі є дублікати категорій з однаковим slug

**Рішення:** Видалити дублікати через API

```javascript
// Отримати список категорій:
const categories = await axios.get('https://strapi.rem-s.com/api/categories?locale=en', {
  headers: { 'Authorization': `Bearer ${token}` }
});

// Знайти дублікати (однаковий slug):
const duplicates = categories.data.data.filter((cat, index, self) =>
  self.findIndex(c => c.attributes.categorySlug === cat.attributes.categorySlug) !== index
);

// Видалити дублікати:
for (const dup of duplicates) {
  await axios.delete(`https://strapi.rem-s.com/api/categories/${dup.id}`, {
    headers: { 'Authorization': `Bearer ${token}` }
  });
}
```

---

### 5. Timeout при відправці

**Симптом:** Скрипт зависає і не завершується

**Причина:** Занадто багато файлів відправляється одночасно

**Рішення:** Використати порційну відправку (див. вище)

---

## 📋 Чеклист перед відправкою

**AI має перевірити ПЕРЕД відправкою:**

- [ ] `categories` - **число**, НЕ масив (детально: `/guides/STRAPI_API_FORMAT.md`)
- [ ] Компоненти (`products`, `responsibilities`, `tools`) - **БЕЗ поля `id`**
- [ ] Якщо файлів 50+ - використати **порційну відправку**
- [ ] `.env` файл з токеном існує
- [ ] Всі зміни в папці `updated/`, НЕ в `exported/`

**Додаткова документація:**
- 📄 `/ENTITIES/ASSETS/rem-s/guides/STRAPI_API_FORMAT.md` - повний гайд по форматах
- 📄 `/ENTITIES/ASSETS/rem-s/updated/collections/vacancies/template/README.md` - формат темплейту вакансії

---

**CSV показує:**
- Дата зміни
- Колекція (vacancies, categories тощо)
- Мова (uk, en, ru, pl)
- ID файлу
- Назва файлу
- Статус (modified/created/deleted)

### Як переглянути звіт

**AI має інформувати користувача після БУДЬ-ЯКОЇ операції (оновлення, додавання, видалення):**

> "✅ Зміни успішно відправлено! Можеш перевірити на сайті rem-s.com"

---

## 🔄 Загальні правила роботи AI

### Правило 1: AI виконує команди САМ (КРИТИЧНО ВАЖЛИВО!)

- AI **НЕ** просить користувача виконувати команди в терміналі
- AI **НЕ** просить користувача відкривати файли в браузері
- AI **САМ** читає всі файли та показує інформацію в чаті
- AI **САМ** виконує всі команди через інструменти
- AI показує користувачу результат виконання
- **Користувач працює ТІЛЬКИ через чат, нічого не пише в терміналі!**

### Правило 2: Навігація по папках

- Коли користувач хоче працювати з вакансіями → AI одразу показує папку `updated/collections/vacancies`
- Після вибору мови → AI показує папку `updated/collections/vacancies/languages/{мова}/vacancies`
- AI завжди надає повні шляхи до файлів

### Правило 3: Не чекай відповіді

- Після кожного питання одразу надавай наступні інструкції або варіанти
- Не пиши "чекаю вашої відповіді" - просто продовжуй
- Після отримання "так/ні" одразу дій

### Правило 4: Конкретні інструкції

- Завжди надавай конкретні шляхи до файлів
- Завжди надавай приклади
- Завжди показуй результат після виконання дій

### Правило 5: Безпека

- Токен налаштований координатором і автоматично завантажується з .env файлу
- НЕ проси користувача надавати токен
- Завжди попереджай про незворотні операції (видалення)
- Завжди показуй список змін перед відправкою

### Правило 6: Обробка помилок

- Якщо помилка - одразу показуй деталі
- Надавай конкретні рішення
- Якщо не знаєш - проси технічну допомогу

---

## 📋 Швидкі відповіді на часті питання

**"Де знайти файл?"**
> Шлях завжди: `C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\updated\collections\{колекція}\languages\{мова}\{колекція}\`

**"Який ID категорії?"**
> Подивіться таблицю вище або файл `TEMPLATE_README.md`

**"Як перевірити JSON?"**
> Скопіюйте вміст на https://jsonlint.com/

**"Токен не працює?"**
> Зверніться до координатора для перевірки та оновлення токену в .env файлі

**"Помилка 401?"**
> Токен неправильний або застарів. Зверніться до координатора для оновлення токену

**"Помилка 403?"**
> У токена немає прав на цю операцію. Зверніться до координатора для перевірки прав токену

**"Помилка 405?"**
> Можливо, неправильний метод або ендпоінт

---

## 🎯 Приклад діалогу 0: АВТОМАТИЧНИЙ ЕКСПОРТ (найпростіший)

**Користувач:** "ENTITIES/ASSETS/rem-s/guides/collections/AI_COLLECTIONS_WORKFLOW.md"

**AI (автоматично перевіряє Node.js та токен):**

> "Перевіряю налаштування..."
> 
> [AI сам виконує: node --version]
> [AI сам перевіряє наявність .env файлу]
> 
> "✅ Node.js встановлено
> ✅ Токен налаштовано
> 
> 👋 Привіт! Готова допомогти з колекціями Strapi.
> 
> **Що ти хочеш зробити?**
> - 📝 Оновити існуючу вакансію
> - ➕ Створити нову вакансію
> - 🗑️ Видалити вакансію
> - 📥 Отримати дані з сайту (експорт)
> - 📤 Відправити зміни на сайт
> - 📊 Переглянути звіти про зміни
> - 🔧 Інше (уточни що саме)"

**Користувач:** "Отримати дані з сайту"

**AI (ОДРАЗУ виконує експорт БЕЗ жодних додаткових питань):**

> "🚀 Виконую експорт даних з сайту..."
> 
> [AI сам виконує: cd ENTITIES/ASSETS/rem-s/scripts/collections-export && node script-collections-export.js]
> 
> "✅ Відмінно! Дані експортовані!
> 
> **Що відбулося:**
> 1. 📂 Оригінали з сайту збережено в `exported/collections/`
> 2. 📂 Автоматично скопійовано в `updated/collections/`
> 
> ⚠️ **ВАЖЛИВО:** Всі зміни робляться ТІЛЬКИ в папці `updated`!
> Зміни відправляються на сайт ТІЛЬКИ з папки `updated`!
> 
> **Готово! Тепер можеш редагувати вакансії.**"

**❌ КОРИСТУВАЧ НЕ ВВОДИВ ЖОДНИХ КОМАНД В ТЕРМІНАЛ!**
**✅ AI ЗРОБИВ ВСЕ САМ!**

---

## 🎯 Приклад діалогу 0.1: АВТОМАТИЧНА ВІДПРАВКА (найпростіший)

**Користувач:** "Відправити зміни на сайт"

**AI (ОДРАЗУ виконує відправку БЕЗ жодних додаткових питань):**

> "🚀 Відправляю зміни на сайт..."
> 
> [AI сам виконує: cd ENTITIES/ASSETS/rem-s/scripts/collections-update && node script-collections-update.js]
> 
> "✅ Відмінно! Зміни успішно відправлені на сервер.
> 
> **Результат:**
> - Створено: 0
> - Оновлено: 3
> - Видалено: 0
> 
> Можеш перевірити зміни на сайті rem-s.com
> 
> **Чи ти завершила редагування?**
> - Так, завершила
> - Ні, продовжую працювати"

**❌ КОРИСТУВАЧ НЕ ВВОДИВ ЖОДНИХ КОМАНД В ТЕРМІНАЛ!**
**✅ AI ЗРОБИВ ВСЕ САМ!**

---

## 🎯 Приклад діалогу 1: Оновлення вакансії

**Користувач:** "Хочу оновити вакансію"

**AI (одразу виконує перевірки автоматично):**

> "Перевіряю налаштування..."
> 
> [AI перевіряє Node.js та наявність .env файлу з токеном]
> 
> "✅ Все готово!
> 
> **Ти вже отримала дані з сайту (виконала експорт)?**
> - Так
> - Ні"

**Користувач:** "Так"

**AI:**

> "**Якою мовою вакансія, яку ти хочеш оновити?**
> - Українська (uk)
> - Російська (ru)
> - Англійська (en)
> - Польська (pl)"

**Користувач:** "Українська"

**AI:**

> "✅ Папка з вакансіями українською:
> 
> **Шлях:** `ENTITIES/ASSETS/rem-s/updated/collections/vacancies/languages/uk/vacancies`
> 
> 📂 **Відкрий цю папку в Cursor Explorer (ліва панель):**
> 1. Знайди папку в дереві файлів
> 2. Знайди потрібну вакансію
> 3. Клікни правою кнопкою на файл → "Copy Path"
> 4. Надішли мені скопійований шлях
> 
> **Приклад шляху:**
> `ENTITIES/ASSETS/rem-s/updated/collections/vacancies/languages/uk/vacancies/1217_front-end.json`"

**Користувач:** "ENTITIES/ASSETS/rem-s/updated/collections/vacancies/languages/uk/vacancies/1217_front-end.json"

**AI (читає та відкриває файл):**

> [AI виконує read_file]
> 
> "✅ Відкрив файл для редагування!
> 
> **Файл:** `1217_front-end.json`
> 
> 📝 Тепер можеш редагувати файл прямо у вкладці Cursor.
> 
> **Коли закінчиш:**
> 1. Збережи зміни (Ctrl+S)
> 2. Напиши мені "готово" або "закінчила"
> 
> І я запитаю, чи відправити зміни на сайт!"

**Користувач (редагує файл у Cursor, потім пише):** "готово"

**AI (одразу оновлює звіти та запитує):**

> [AI виконує track-changes.js]
> 
> "✅ Прийняла зміни!
> 
> 
> Файл готовий до відправки на сервер.
> 
> **Чи ти бажаєш одразу відправити змінені файли на сайт чи продовжити редагування?**
> - Так, відправити на сайт
> - Ні, продовжити редагувати"

**Користувач:** "Так, відправити"

**AI (відправляє зміни):**

> [AI виконує script-collections-update.js]
> 
> "✅ Відмінно! Зміни успішно відправлені на сервер.
> 
> 📊 Звіти оновлено.
> 
> **Чи ти завершила редагування?**
> - Так, завершила
> - Ні, продовжую працювати"

**Користувач:** "Так, завершила"

**AI:**

> "✅ Дякую за роботу! 🎉
> 
> Можеш перевірити зміни на сайті rem-s.com"

---

## 🎯 Приклад діалогу 2: Створення нової вакансії

**Користувач:** "Хочу створити нову вакансію"

**AI:**

> "✅ Все готово!
> 
> **Ти вже отримала дані з сайту (виконала експорт)?**
> - Так
> - Ні"

**Користувач:** "Так"

**AI:**

> "**Якою мовою буде вакансія?**
> - Українська (uk)
> - Російська (ru)
> - Англійська (en)
> - Польська (pl)"

**Користувач:** "Українська"

**AI (показує структуру папки):**

> [AI виконує: cd та показує список файлів]
> 
> "✅ Перейшла в папку з українськими вакансіями.
> 
> 📂 **Структура папки:**
> {список файлів}
> 
> ⚠️ **ВАЖЛИВО:** Це папка `updated` - тут створюється нова вакансія!
> 
> 📝 **Надай, будь ласка, матеріали для нової вакансії:**
> 
> 1. **Назва вакансії** (title)
> 2. **Короткий опис для картки** (cardDescription)
> 3. **Заголовок H1** (titleH1)
> 4. **Підзаголовок** (subTitle)
> 5. **Детальний опис** (description)
> 6. **Категорія** (translators, developers, designers, managers, marketers, tutors, other)
> 7. **Slug** (vacancySlug)
> 
> **ID категорій (однакові для всіх мов: uk, en, ru, pl):**
> - translators: 49
> - developers: 48
> - designers: 47
> - managers: 50
> - marketers: 51
> - tutors: 52
> - other: 53
> 
> **КРИТИЧНО ВАЖЛИВО про формат полів у темплейті:**
> 
> ✅ **ПРАВИЛЬНИЙ формат для Strapi API:**
> 
> ```json
> {
>   "id": null,
>   "attributes": {
>     "title": "Назва вакансії",
>     "description": "...",
>     "locale": "uk",
>     "vacancySlug": "vacancy-slug",
>     "cardDescription": "...",
>     "titleH1": "...",
>     "subTitle": "...",
>     "isHot": false,
>     "newVersion": null,
>     "productsTitle": null,
>     "responsibilityTitle": null,
>     "toolsTitle": null,
>     "formTitle": "",
>     "videoLink": "",
>     "categories": [48],
>     "keyword_tags": [],
>     "skills": [],
>     "videoPreview": null,
>     "products": [],
>     "responsibilities": [],
>     "tools": [],
>     "seoData": null,
>     "localizations": []
>   }
> }
> ```
> 
> ❌ **НЕПРАВИЛЬНІ формати (призведуть до помилки 500):**
> 
> ```json
> // НЕ ТАК - категорії як об'єкти:
> "categories": { "data": [{ "id": 48 }] }
> 
> // НЕ ТАК - категорії з attributes:
> "categories": { "data": [{ "id": 48, "attributes": {...} }] }
> 
> // НЕ ТАК - id: null в категоріях:
> "categories": { "data": [{ "id": null }] }
> ```
> 
> **ПРАВИЛО:** Relations (categories, keyword_tags, skills, localizations) - це масиви ID, БЕЗ обгорток "data" та БЕЗ "attributes"!"

**Користувач:** "[надає всі дані]"

**AI (створює файл):**

> [AI читає темплейт, заповнює даними, створює файл]
> 
> "✅ Файл вакансії створено!
> 
> **Шлях:** `ENTITIES/ASSETS/rem-s/updated/collections/vacancies/languages/uk/vacancies/9999_{slug}.json`
> 
> **Чи ти бажаєш одразу відправити змінені файли на сайт чи продовжити редагування?**
> - Так, відправити на сайт
> - Ні, продовжити редагувати"

**Користувач:** "Так, відправити"

**AI (відправляє та оновлює звіт):**

> [AI виконує script-collections-update.js → автоматично запускається track-changes.js]
> 
> "✅ Відмінно! Зміни успішно відправлені на сервер.
> 
> Можеш перевірити зміни на сайті rem-s.com
> 
> **Дякую за роботу! 🎉**"

---

**Останнє оновлення:** 2025-12-02



