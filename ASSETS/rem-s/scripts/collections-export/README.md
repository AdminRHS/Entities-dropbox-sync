# Strapi Collections Export Script

## Опис

Універсальний скрипт для експорту всіх колекцій з Strapi CMS на `https://strapi.rem-s.com` у форматі JSON.

**Особливості:**
- ✅ Експорт всіх колекцій за один запуск
- ✅ Автоматичне визначення локалізованих колекцій
- ✅ Підтримка мультилокальності (ru, en, uk, pl) для локалізованих колекцій
- ✅ Автоматичне очищення старих файлів при новому експорті
- ✅ Не створює порожні папки - тільки якщо є дані
- ✅ Паралельний експорт для максимальної продуктивності
- ✅ Мінімальне навантаження на систему

## Вимоги

- Node.js (версія 14 або вище)
- npm або yarn

## Встановлення

```bash
cd C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\scripts\collections-export
npm install
```

## Отримання API токену

1. Увійдіть в Strapi адмін-панель: https://strapi.rem-s.com/admin
2. Перейдіть в Settings → API Tokens
3. Створіть новий токен з правами `Read` для всіх колекцій
4. Скопіюйте токен

## Використання

### Основна команда

**Для Windows CMD або PowerShell:**
```bash
cd C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\scripts\collections-export
node script-collections.js YOUR_TOKEN_HERE
```

**Для Git Bash (MINGW64) - використовуйте відносний або Unix-шлях:**
```bash
# Якщо ви вже в папці scripts:
cd collections-export
node script-collections.js YOUR_TOKEN_HERE

# Або з кореня Dropbox:
cd ENTITIES/ASSETS/rem-s/scripts/collections-export
node script-collections.js YOUR_TOKEN_HERE

# Або повний Unix-шлях:
cd /c/Users/Аня/Desktop/Dropbox/ENTITIES/ASSETS/rem-s/scripts/collections-export
node script-collections.js YOUR_TOKEN_HERE
```

**Через змінну оточення:**

Windows PowerShell:
```powershell
$env:STRAPI_TOKEN="YOUR_TOKEN_HERE"
node script-collections.js
```

Windows CMD:
```cmd
set STRAPI_TOKEN=YOUR_TOKEN_HERE
node script-collections.js
```

Linux/Mac:
```bash
export STRAPI_TOKEN="YOUR_TOKEN_HERE"
node script-collections.js
```

### Що робить скрипт

При запуску скрипт автоматично:

1. ✅ **Очищає старі дані** - видаляє ВСІ старі файли перед новим експортом
2. ✅ **Експортує всі колекції** - завантажує актуальні дані з Strapi для всіх колекцій
3. ✅ **Визначає локалізацію** - автоматично експортує локалізовані колекції для всіх мов
4. ✅ **Записує в існуючі папки** - використовує існуючі папки, не створює нові
5. ✅ **Не створює порожні папки** - якщо колекція порожня, нічого не створюється
6. ✅ **Без дублювання** - кожен файл записується один раз
7. ✅ **Створює файли списків** - автоматично створює/оновлює файли списків
8. ✅ **Очищає порожні папки** - видаляє порожні папки після експорту

> **Важливо:** Один скрипт виконує все автоматично. Не потрібно запускати окремі скрипти для кожної колекції.

### Тестовий режим (перевірка без експорту)

```bash
node script-collections.js --test
```

Перевіряє:
- Доступність Strapi API
- Правильність ендпоінтів
- Створення структури директорій

### Експорт зі збереженням старих файлів (опціонально)

```bash
node script-collections.js YOUR_TOKEN_HERE --keep-old
```

**Коли використовувати:**
- Для порівняння версій
- Для резервного копіювання перед оновленням
- Для аналізу змін

## Структура експортованих файлів

Скрипт створює файли в директорії:
```
C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\exported\collections\
```

**Повна структура директорій:**

```
collections/
├── vacancies/                              # Локалізована колекція
│   └── languages/
│       ├── ru/
│       │   ├── vacancies-list.json
│       │   └── vacancies/
│       │       └── {id}_{title}.json
│       ├── en/
│       │   ├── vacancies-list.json
│       │   └── vacancies/
│       │       └── {id}_{title}.json
│       ├── uk/
│       │   └── ...
│       └── pl/
│           └── ...
│
├── categories/                              # Локалізована колекція
│   └── languages/
│       ├── ru/
│       │   ├── categories-list.json
│       │   └── categories/
│       │       └── {id}_{name}.json
│       └── ...
│
├── skills/                                  # Локалізована колекція
│   └── languages/
│       └── ...
│
├── keyword-tags/                            # Локалізована колекція
│   └── languages/
│       └── ...
│
├── form-users/                              # Локалізована колекція
│   └── languages/
│       └── ...
│
├── users/                                   # Нелокалізована колекція
│   ├── users-list.json
│   └── users/
│       └── {id}_{username}.json
│
├── submissions/                             # Нелокалізована колекція
│   ├── submissions-list.json
│   └── submissions/
│       └── {id}_{title}.json
│
└── recipients/                              # Нелокалізована колекція
    └── ...
```

**Важливо:**
- **Локалізовані колекції** (vacancies, categories, skills, keyword-tags, form-users) мають структуру `languages/{locale}/`
- **Нелокалізовані колекції** (users, submissions, recipients) зберігаються безпосередньо в папці колекції
- Кожна колекція має файл списку (`{collection}-list.json`) та папку з окремими файлами
- Структура створюється автоматично при першому запуску
- Порожні папки не створюються - тільки якщо є дані

### Формат файлів

**Список колекції (`{collection}-list.json`):**
- Містить метадані всіх записів колекції
- Включає: ID, title/name, slug, дати
- Використовується для швидкого пошуку та навігації

**Окремі записи (`{collection}/{id}_{title}.json`):**
- Повна інформація про запис в форматі JSON
- Містить всі поля з Strapi (attributes, зв'язки, тощо)

## Приклад виводу

```
🚀 Starting collections export from Strapi

📍 Strapi URL: https://strapi.rem-s.com
📁 Export directory: C:\...\exported\collections
📦 Collections: vacancies, categories, form-users, keyword-tags, skills, submissions, recipients, users, audiences
🌐 Localized collections: vacancies, categories, keyword-tags, skills, form-users

🧹 Cleaning all old data before export...

📥 Fetching collections from Strapi...

📦 Exporting vacancies RU...

   📁 Export directory: ...\collections\vacancies\languages\ru\vacancies
   📄 List file: ...\collections\vacancies\languages\ru\vacancies-list.json

   vacancies RU: Page 1/1: 38 items (Total: 38/38)

   📦 Found 38 vacancies RU items

   💾 Exported 38 items (0.42s)
   ✓ Verified: 38 JSON files

   📋 Created list: vacancies-list.json

... (аналогічно для інших мов та колекцій)

✅ Export completed!

📊 Summary:

   VACANCIES:
      RU: 38 items, 38 files
      EN: 35 items, 35 files
      UK: 32 items, 32 files
      PL: 28 items, 28 files
      TOTAL: 133 items, 133 files

   CATEGORIES:
      RU: 7 items, 7 files
      EN: 7 items, 7 files
      UK: 7 items, 7 files
      PL: 7 items, 7 files
      TOTAL: 28 items, 28 files

   ... (інші колекції)

   ⏱️  Total time: 5.23s
   📦 Total items: 450
   📄 Total files: 450
   📁 Export directory: C:\...\collections

🧹 Cleaning up empty directories...

   ✓ Cleanup completed
```

## Налаштування

### Додавання нової колекції

Відредагуйте масив `COLLECTIONS` в скрипті:

```javascript
const COLLECTIONS = [
  'vacancies',
  'categories',
  'your-new-collection'  // Додайте тут
];
```

### Додавання локалізованої колекції

Додайте назву колекції в масив `LOCALIZED_COLLECTIONS`:

```javascript
const LOCALIZED_COLLECTIONS = [
  'vacancies', 
  'categories', 
  'your-localized-collection'  // Додайте тут
];
```

### Зміна розміру сторінки

Відредагуйте параметр `pagination[pageSize]` в функції `fetchAllItems()`:

```javascript
params: {
  'pagination[page]': page,
  'pagination[pageSize]': 100,  // Змініть тут
  'populate': '*'
}
```

## Обробка помилок

### Помилка авторизації

```
❌ API Error (collection, Page 1): 401
```

**Рішення:** Перевірте правильність токену та його права доступу до всіх колекцій.

### Помилка мережі

```
❌ Network Error: connect ECONNREFUSED
```

**Рішення:** Перевірте доступність `https://strapi.rem-s.com` та інтернет-з'єднання.

### Колекція не знайдена

```
⚠️  Unexpected response structure for collection-name
```

**Рішення:** Перевірте правильність назви колекції в масиві `COLLECTIONS`.

### Помилка: "Cannot find module 'axios'"

**Рішення:** Запустіть `npm install` в директорії скрипта

### Помилка: "Cannot find module 'script-collections.js'"

**Рішення:** Переконайтеся, що ви знаходитесь в правильній директорії:
```bash
cd C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\scripts\collections-export
```

### Помилка: "No such file or directory" (Git Bash)

**Рішення:** Використовуйте відносний або Unix-шлях замість Windows-шляху:
```bash
# Замість C:\Users\... використовуйте:
cd /c/Users/Аня/Desktop/Dropbox/ENTITIES/ASSETS/rem-s/scripts/collections-export
```

## Безпека

⚠️ **Важливо:** Не зберігайте токен в коді або в системі контролю версій!

- Використовуйте змінні оточення
- Додайте `.env` файл в `.gitignore`
- Використовуйте токени з мінімальними правами (Read-only)

## Підтримка

При виникненні проблем:

1. Перевірте правильність токену
2. Перевірте доступність Strapi API
3. Перевірте права доступу до директорії експорту
4. Перегляньте повідомлення про помилки в консолі
5. Запустіть тестовий режим: `node script-collections.js --test`
6. Переконайтеся, що ви в правильній директорії (використовуйте `pwd` або `cd`)

## Ліцензія

Внутрішній скрипт для проекту rem-s.com
