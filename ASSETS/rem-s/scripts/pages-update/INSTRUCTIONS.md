# Інструкція: Як оновлювати дані в JSON файлах

## ⚠️ ВАЖЛИВО: Де редагувати дані

Дані для оновлення в Strapi **ЗАВЖДИ** знаходяться на **КОРЕНЕВОМУ РІВНІ** JSON файлу, а **НЕ** в `localizations`!

## ✅ Поля, які можна редагувати (на кореневому рівні):

```json
{
  "firstDescription": "...",           ← РЕДАГУЙТЕ ТУТ
  "title": "...",                       ← РЕДАГУЙТЕ ТУТ
  "videoUrl": "...",                    ← РЕДАГУЙТЕ ТУТ
  "WhatWeDoTitle": "...",               ← РЕДАГУЙТЕ ТУТ
  "SecondDescription": "...",           ← РЕДАГУЙТЕ ТУТ
  "aboutCompanyTitle": "...",           ← РЕДАГУЙТЕ ТУТ
  "whyUsTitle": "...",                  ← РЕДАГУЙТЕ ТУТ
  "aboutCompanyDescription": "...",     ← РЕДАГУЙТЕ ТУТ
  "whyUsDescription": "...",            ← РЕДАГУЙТЕ ТУТ
  "joinText": "...",                    ← РЕДАГУЙТЕ ТУТ
  "learnMoreText": "...",               ← РЕДАГУЙТЕ ТУТ
  "features": [...],                    ← РЕДАГУЙТЕ ТУТ
  
  // ❌ НЕ РЕДАГУЙТЕ ЦІ ПОЛЯ - вони ігноруються:
  "createdAt": "...",                   ← ІГНОРУЄТЬСЯ
  "updatedAt": "...",                   ← ІГНОРУЄТЬСЯ
  "locale": "...",                      ← ІГНОРУЄТЬСЯ
  "localizations": { ... }              ← ІГНОРУЄТЬСЯ (це дані для інших локалей)
}
```

## 📝 Приклад правильного редагування:

### ✅ ПРАВИЛЬНО:
```json
{
  "title": "RemotEmployees - Firma Outstaffing - НОВИЙ ТЕКСТ",
  "firstDescription": "НОВИЙ ОПИС...",
  ...
}
```

### ❌ НЕПРАВИЛЬНО (не працює):
```json
{
  "title": "Старий текст",
  "localizations": {
    "data": [{
      "attributes": {
        "title": "НОВИЙ ТЕКСТ"  ← ЦЕ НЕ ПРАЦЮЄ!
      }
    }]
  }
}
```

## 🔍 Як перевірити, що дані читаються правильно:

Після запуску скрипта в логах ви побачите:
```
📄 З файлу title: "RemotEmployees - Firma Outstaffing"
📤 Відправляється 12 полів з файлу
```

Якщо ви не бачите свої зміни в логах `📄 З файлу ...`, значить:
1. Або ви редагували не ті поля
2. Або файл не збережено правильно

## 📋 Список всіх полів, які оновлюються:

1. `firstDescription` - Перший опис
2. `title` - Заголовок
3. `videoUrl` - URL відео
4. `WhatWeDoTitle` - Заголовок "Що ми робимо"
5. `SecondDescription` - Другий опис
6. `aboutCompanyTitle` - Заголовок про компанію
7. `whyUsTitle` - Заголовок "Чому ми"
8. `aboutCompanyDescription` - Опис компанії
9. `whyUsDescription` - Опис "Чому ми"
10. `joinText` - Текст кнопки "Приєднатися"
11. `learnMoreText` - Текст "Дізнатися більше"
12. `features` - Масив об'єктів з характеристиками

## ⚠️ Важливі зауваження:

- **НЕ редагуйте** поля `createdAt`, `updatedAt`, `locale` - вони автоматично оновлюються
- **НЕ редагуйте** `localizations` - це дані для інших локалей (en, uk, ru, sk)
- **ЗАВЖДИ** редагуйте дані на **КОРЕНЕВОМУ РІВНІ** файлу
- Після редагування **збережіть файл** перед запуском скрипта

## 🔧 Як перевірити, чи файл дійсно збережений:

Якщо ви зберігаєте файл (Ctrl+S), але зміни не зберігаються:

1. **Перевірте індикатор збереження в редакторі:**
   - У VS Code/Cursor подивіться на вкладку файлу - якщо є крапка (●), файл не збережений
   - Натисніть `Ctrl+S` кілька разів

2. **Закрийте і відкрийте файл знову:**
   - Закрийте файл (Ctrl+W)
   - Відкрийте його знову
   - Перевірте, чи містить він ваші зміни

3. **Перевірте файл в іншому редакторі:**
   - Відкрийте файл в Notepad або іншому редакторі
   - Перевірте, чи містить він ваші зміни

4. **Перевірте права доступу:**
   - Можливо, файл заблокований або немає прав на запис
   - Спробуйте зберегти файл під іншим ім'ям, потім перейменуйте

5. **Перевірте логи скрипта:**
   - На початку скрипт показує, що саме читається з файлів
   - Якщо показані старі значення - файл не збережений
   - Скрипт дає 5 секунд для збереження файлів перед продовженням

