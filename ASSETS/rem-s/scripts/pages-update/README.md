# Strapi Single Types Update Script

Скрипт для оновлення single types в Strapi CMS з файлів з папки `updated/pages/`.

## ⚠️ Важливо

**Цей скрипт працює з ПРОДАКШН базою даних!** Використовуйте з обережністю.

## Покращення версії 2.0

- ✅ Винесена логіка спроб у окремий модуль (`strapi-api-retry.js`)
- ✅ Валідація токена перед використанням
- ✅ Покращена обробка помилок GET запитів
- ✅ Обмеження на кількість спроб (максимум 12)
- ✅ Перевірка успішності backup з опцією `--force`
- ✅ Детальне логування кожної спроби оновлення
- ✅ Виправлена обробка `publishedAt` поля

## Встановлення

```bash
npm install
```

## Структура папки updated/pages/

Скрипт читає файли з такої структури:

```
updated/pages/
  About/
    en.json
    uk.json
    pl.json
    ru.json
  Contact/
    en.json
    ...
```

Файли мають бути у форматі пласких JSON об'єктів (без `data.attributes`), такий самий як після експорту.

## Використання

### Dry-run режим (за замовчуванням)

Перегляд того, що буде оновлено, без реальних змін:

```bash
node strapi-single-types-update.js --dry-run
```

### Реальне оновлення з підтвердженням

```bash
node strapi-single-types-update.js --confirm <token>
```

або через environment variable:

```bash
STRAPI_TOKEN=your_token_here node strapi-single-types-update.js --confirm
```

### Оновлення з backup та експортом після

```bash
node strapi-single-types-update.js --confirm --backup --export-after <token>
```

### Оновлення з примусовим продовженням без backup

```bash
node strapi-single-types-update.js --confirm --backup --force <token>
```

## Опції

- `--dry-run` - Тільки перегляд без реальних змін (за замовчуванням, якщо не вказано `--confirm`)
- `--confirm` - Підтвердження для реальних оновлень (обов'язково для змін в Strapi)
- `--backup` - Створення backup (експорт) перед оновленням
- `--export-after` - Запуск експорту після успішного оновлення
- `--force` - Продовжити навіть якщо backup не створено (використовуйте з обережністю!)

## Процес оновлення

1. **Валідація токена** - Перевірка валідності токена через `/api/users/me`
2. **Сканування** - Скрипт сканує папку `updated/pages/` та знаходить всі файли для оновлення
3. **Валідація** - Перевірка структури JSON та наявності даних
4. **Підтвердження** - Якщо використано `--confirm`, чекає 5 секунд перед оновленням
5. **Backup** - Якщо використано `--backup`, створює backup через експорт (з перевіркою успішності)
6. **Оновлення** - Оновлює кожен файл через Strapi API з множинними спробами та затримкою 500ms між запитами
7. **Експорт** - Якщо використано `--export-after`, запускає експорт після оновлення

## Логування

Всі дії логуються у файл:

```
logs/update-{timestamp}.log
```

Тепер логуються також:
- Кожна спроба оновлення (успішна або невдала)
- Помилки GET запитів (окрім 404)
- Деталі помилок API

Формат логу:
```
[2024-11-28T14:30:15.123Z] UPDATE About/en.json → SUCCESS
[2024-11-28T14:30:15.456Z] 🔄 Спроба 1: PUT standard
[2024-11-28T14:30:15.789Z] ✅ Успіх на спробі 1 (PUT, standard)
```

## API Endpoint

Скрипт використовує множинні спроби з різними комбінаціями:
- URL формати: `/api/{endpoint}` та `/api/single-types/{endpoint}`
- HTTP методи: PUT, PATCH
- Body формати: стандартний та з поточними даними
- Config варіанти: базовий та з `publicationState: 'live'`

**Headers:**
```
Authorization: Bearer {token}
Content-Type: application/json
```

**Body:** JSON об'єкт з обгорткою `data`:
```json
{
  "data": {
    // дані для оновлення
  }
}
```

## Безпека

- **Dry-run за замовчуванням** - без `--confirm` скрипт не робить реальних змін
- **Валідація токена** - перевірка валідності токена перед використанням
- **Валідація файлів** - перевірка структури файлів перед оновленням
- **Перевірка backup** - скрипт не продовжить без backup (окрім `--force`)
- **Rate limiting** - затримка 500ms між запитами
- **Обмеження спроб** - максимум 12 спроб для кожного оновлення
- **Обробка помилок** - детальні повідомлення про помилки

## Приклади

### Приклад 1: Перегляд змін

```bash
node strapi-single-types-update.js --dry-run
```

### Приклад 2: Реальне оновлення з валідацією токена

```bash
node strapi-single-types-update.js --confirm your_token_here
```

Вивід:
```
🔐 Перевірка токена...
✅ Токен валідний (користувач ID: 1)

⚠️  УВАГА: Буде виконано реальне оновлення в Strapi!
   Натисніть Ctrl+C для скасування або зачекайте 5 секунд...

[1/3] Оновлення About/en.json...
   🔄 Спроба 1: PUT standard
   ✅ Успіх на спробі 1 (PUT, standard)
```

### Приклад 3: Повний цикл з backup та експортом

```bash
node strapi-single-types-update.js --confirm --backup --export-after your_token_here
```

### Приклад 4: Примусове продовження без backup

```bash
node strapi-single-types-update.js --confirm --backup --force your_token_here
```

⚠️ **Увага**: Використовуйте `--force` тільки якщо ви впевнені, що backup не критичний!

## Обробка помилок

Скрипт обробляє такі помилки:

- **Невірний формат JSON** - файл не може бути розпарсений
- **Порожній файл** - файл не містить даних
- **Невалідний токен** - токен не проходить перевірку
- **API помилки** - помилки від Strapi API (401, 404, 500, тощо) з множинними спробами
- **Мережеві помилки** - проблеми з підключенням
- **Помилки backup** - якщо backup не створено, скрипт зупиняється (окрім `--force`)

Всі помилки логуються у файл та виводяться у консоль з деталями кожної спроби.

## Статистика

Після завершення скрипт виводить статистику:

```
✅ Оновлення завершено!

📊 Підсумок:
   Успішно оновлено: 8
   Помилки: 2
   Log file: logs/update-2024-11-28T14-30-15-123Z.log
```

## Рекомендації

1. **Завжди використовуйте `--dry-run` спочатку** для перевірки
2. **Створюйте backup** перед оновленням (`--backup`)
3. **Експортуйте після оновлення** (`--export-after`) для синхронізації
4. **Перевіряйте логи** після кожного оновлення
5. **Тестуйте на невеликій кількості файлів** перед масовим оновленням
6. **Не використовуйте `--force`** без крайньої необхідності

## Troubleshooting

### Токен невалідний

Перевірте:
- Чи токен правильний
- Чи токен не застарів
- Чи токен має необхідні права доступу

### Файли не знаходяться

Перевірте:
- Чи існує папка `updated/pages/`
- Чи правильна структура папок (`updated/pages/{Entity}/{locale}.json`)
- Чи файли мають правильні назви (`en.json`, `uk.json`, тощо)

### Помилки API

- Перевірте токен доступу
- Перевірте чи endpoint правильний
- Перевірте формат даних у JSON файлах
- Перевірте логи для деталей кожної спроби

### Backup не створюється

- Перевірте чи скрипт експорту працює (`../pages-export/strapi-single-types-export.js`)
- Перевірте права доступу до папок
- Використайте `--force` тільки якщо впевнені

