# 📝 Як оновити дані на сайті (дуже просто!)

## 🎯 Що це?

Цей скрипт допомагає відправити ваші зміни на сайт rem-s.com. Він сам визначає, що ви змінили, і відправляє правильні команди на сервер.

---

## 🚀 Швидкий старт (3 кроки)

### Крок 1: Отримати дані з сайту

Відкрийте командний рядок (Win + R → `cmd` → Enter) і виконайте:

```bash
cd C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\scripts\collections-export
node script-collections-export.js YOUR_TOKEN
```

**Що відбувається:** Всі дані з сайту завантажуються в папку `exported/collections/` і автоматично копіюються в `updated/collections/`.

**Готово!** ✅ Тепер ви можете редагувати файли.

---

### Крок 2: Відредагувати файли

1. Відкрийте папку: `C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\updated\collections\`
2. Знайдіть потрібний файл (наприклад, вакансію)
3. Відкрийте файл у Notepad++ або VS Code
4. Змініть текст
5. Збережіть файл (Ctrl+S)

**⚠️ ВАЖЛИВО:**
- Не видаляйте лапки `"` та коми `,`
- Не змінюйте число в назві файлу (наприклад, `1432_`)
- Не видаляйте поля

**Приклад зміни:**
```json
Було:  "title": "Polish Translator"
Стало: "title": "Senior Polish Translator"
```

---

### Крок 3: Відправити зміни на сайт

#### Спочатку перегляньте, що буде змінено (без застосування):

```bash
cd C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\scripts\collections-update
node script-collections-update.js YOUR_TOKEN --dry-run
```

**Що відбувається:** Скрипт покаже, що буде оновлено, створено або видалено, але **НЕ ЗАСТОСУЄ** зміни.

**Перевірте:** Якщо все правильно → переходите далі. Якщо є помилки → виправте їх.

#### Якщо все правильно, застосуйте зміни:

```bash
node script-collections-update.js YOUR_TOKEN
```

**⚠️ УВАГА:** Це застосує зміни на сайті!

**Готово!** ✅ Дані оновлені на сайті.

---

## 📋 Що робить скрипт?

Скрипт дуже розумний! Він сам визначає:

### ➕ Якщо ви створили новий файл
→ Скрипт відправить команду **POST** (створити новий запис на сайті)

### ✏️ Якщо ви змінили існуючий файл
→ Скрипт відправить команду **PUT** або **PATCH** (оновити запис на сайті)

### 🗑️ Якщо ви видалили файл
→ Скрипт відправить команду **DELETE** (видалити запис з сайту)

---

## ⚙️ Як працює скрипт (технічні деталі)

### 🛡️ Захист сервера

Скрипт дуже обережний, щоб не перевантажити сервер:

- ⏱️ **Затримка між запитами:** 800 мілісекунд (щоб сервер не "ліг")
- 📦 **Обробка батчами:** По 10 файлів за раз
- 🔄 **Повтор при помилці:** До 3 спроб з затримкою
- ⏸️ **Пауза між батчами:** Додаткова пауза між групами файлів

### 📊 Що показує скрипт

Після виконання ви побачите:

```
📊 Знайдені зміни:
   ➕ Створено: 2
   ✏️  Оновлено: 5
   🗑️  Видалено: 1
   📦 Всього змін: 8

📦 Обробка батчу 1/1 (8 файлів)...
   ✅ Створено: vacancies #1433 (en) [POST]
   ✅ Оновлено: vacancies #1432 (en) [PUT]
   ✅ Видалено: vacancies #1431 (en)

📊 Підсумок:
   ➕ Створено: 2 успішно, 0 помилок
   ✏️  Оновлено: 5 успішно, 0 помилок
   🗑️  Видалено: 1 успішно, 0 помилок
   📦 Всього оброблено: 8
```

---

## 📁 Де що знаходиться?

### Папка `exported/collections/`
- 📥 **Для читання:** Оригінальні дані з сайту
- ⚠️ **Не редагуйте тут!** Це тільки для перегляду

### Папка `updated/collections/`
- ✏️ **Для редагування:** Тут ви редагуєте файли
- 📤 **Звідси відправляються зміни:** Скрипт читає файли звідси

**Важливо:** Структура папок має бути однакова!

**Приклад:**
```
exported/collections/vacancies/languages/en/vacancies/1432_polish-translator.json
updated/collections/vacancies/languages/en/vacancies/1432_polish-translator.json
```

### ⚠️ Файли, які автоматично ігноруються:

Скрипт **автоматично пропускає** такі файли:
- 📁 Папки `template` або `templates` (повністю ігноруються)
- 📄 Файли без ID в назві (наприклад, `vacancy-template.json` замість `1432_polish-translator.json`)
- 📄 Файли списків (з `-list.json` в назві)
- 📄 Приховані файли (що починаються з `.`)

**Це означає:** Ви можете зберігати template файли в папці `template` - вони ніколи не будуть відправлені на сервер! ✅

---

## 🔍 Режим перегляду (Dry-Run)

**Що це?** Режим, де скрипт показує, що буде зроблено, але **НЕ ЗАСТОСОВУЄ** зміни.

**Як використати:**
```bash
node script-collections-update.js YOUR_TOKEN --dry-run
```

**Коли використовувати:**
- ✅ Перед першим оновленням
- ✅ Коли хочете перевірити, що все правильно
- ✅ Коли навчаєтеся працювати зі скриптом

**Що ви побачите:**
```
🔍 Режим DRY-RUN - зміни не будуть застосовані

📊 Знайдені зміни:
   ➕ Створено: 2
   ✏️  Оновлено: 5
   🗑️  Видалено: 1

📦 Обробка батчу 1/1 (8 файлів)...
   🔍 [DRY-RUN] Створення нового vacancies (en)
   🔍 [DRY-RUN] Оновлення vacancies #1432 (en)
   🔍 [DRY-RUN] Видалення vacancies #1431 (en)

💡 Для застосування змін запустіть без --dry-run
```

---

## ❓ Що робити, якщо помилка?

### Помилка: "API токен обов'язковий!"
**Рішення:** Додайте токен:
```bash
node script-collections-update.js YOUR_TOKEN
```

### Помилка: "Snapshot не знайдено"
**Рішення:** Спочатку запустіть скрипт експорту:
```bash
cd C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\scripts\collections-export
node script-collections-export.js YOUR_TOKEN
```

### Помилка: "Файлів для оновлення не знайдено"
**Рішення:** Перевірте, чи файли знаходяться в правильній папці:
```
C:\Users\Аня\Desktop\Dropbox\ENTITIES\ASSETS\rem-s\updated\collections\
```

### Помилка: "Помилка API: 401"
**Рішення:** Токен неправильний або застарів. Отримайте новий токен.

### Помилка: "Помилка API: 429" (Too Many Requests)
**Рішення:** Сервер перевантажений. Скрипт автоматично повторить спробу через кілька секунд.

### Помилка: "Помилка API: 500"
**Рішення:** Проблема на сервері. Скрипт автоматично повторить спробу. Якщо помилка повторюється, зверніться до технічного спеціаліста.

---

## ✅ Чек-лист перед оновленням

- [ ] Отримано дані з сайту (скрипт експорту)
- [ ] Файли автоматично скопійовані в `updated/collections/`
- [ ] Файли відредаговані та збережені
- [ ] Запущено перегляд змін (`--dry-run`)
- [ ] Перевірено, що все правильно
- [ ] Відправлено зміни на сайт (без `--dry-run`)

**Готово! 🎉**

---

## 📞 Допомога

Якщо щось не працює:
1. Перевірте, чи правильно виконані всі кроки
2. Перевірте структуру папок
3. Перевірте формат JSON файлів
4. Зверніться до технічного спеціаліста

---

## 🔧 Технічні параметри

Якщо потрібно змінити налаштування, відредагуйте файл `script-collections-update.js`:

```javascript
const RATE_LIMIT_DELAY = 800;  // Затримка між запитами (мс)
const BATCH_SIZE = 10;         // Розмір батчу (файлів)
const MAX_RETRIES = 3;         // Максимум повторів
const RETRY_DELAY = 2000;      // Затримка перед повтором (мс)
```

**⚠️ Не змінюйте, якщо не знаєте, що робите!**
